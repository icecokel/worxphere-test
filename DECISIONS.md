# 설계 결정 기록

## D-001. 프로젝트 기반 기술

- **결정:** Next.js App Router, TypeScript, Tailwind CSS, shadcn/ui를 사용한다.
- **이유:** 과제에서 허용한 React 계열 스택이며, 사용자가 프로젝트 기반 기술로 지정했다.

## D-002. Mock API 방식

- **결정:** 브라우저 환경의 MSW를 사용한다.
- **이유:** 실제 백엔드 없이 네트워크 요청과 응답 지연·실패를 시뮬레이션해야 한다.
- **구현 원칙:** 초기 요청과 worker 등록의 경쟁을 막기 위해 worker 시작 완료 후 애플리케이션을 표시한다.

## D-003. 기능 단위 커밋 보호 방식

- **결정:** 의미 판단은 프로젝트 스킬로 안내하고, 확인 가능한 규칙만 Git 훅으로 차단한다.
- **커밋 훅:** 기능 커밋의 `feature-key`와 `PROMPTS.md` 섹션 및 필수 기록 내용을 확인한다.
- **푸시 훅:** 원격 브랜치를 덮어쓰는 비 fast-forward push를 차단한다.
- **한계:** 기능 경계와 기록의 진실성, squash 여부는 안정적으로 자동 판별할 수 없으므로 작업자가 최종 확인한다.

## D-004. 파이프라인 화면 구성

- **결정:** 나인하이어 ATS의 단계별 컬럼과 카드형 보드를 레퍼런스로 사용하되, 상세 패널을 `다음 채용 단계`와 `불합격 처리`로 나눈다. 정규 전이는 `서류검토 → 면접 → 처우협의 → 최종합격`의 다음 단계만 허용하고, 불합격은 `서류검토`, `면접`, `처우협의`에서만 별도 액션으로 처리한다. `최종합격`과 `불합격`은 종료 상태다.
- **이유:** 단계 변경은 지원자에게 영향을 주고 실제 서비스에서는 알림이 이어질 수 있는 중요한 결정이므로, 드래그앤드롭보다 대상과 변경 내용을 명시적으로 검토하는 흐름이 안전하다.
- **적용 범위:** 정보 구조만 참고하며 공고 관리, 평가, 메시지, 일정과 실제 알림 발송 기능은 포함하지 않는다. 일반 ATS에서 불합격을 별도로 관리하더라도 과제 요구사항에 따라 `불합격` 컬럼을 유지한다.

## D-005. UI 디자인 기준

- **결정:** 디자인은 주요 평가 범위로 확장하지 않고 shadcn/ui의 `base-nova`, `neutral` 기본 디자인과 기존 semantic token을 적극 활용한다.
- **이유:** 별도 디자인 시스템을 만들지 않고 기능 정확성과 상태 처리에 집중하면서 화면 일관성을 유지한다.
- **적용 원칙:** 나인하이어는 정보 구조만 참고한다. 요구사항이 없는 커스텀 색상, 폰트, 그림자, radius, animation은 추가하지 않는다.

## D-006. Mock API 계약과 오류 우선순위

- **결정:** 공통 지원자 스키마를 요청·응답 스키마가 재사용하고, 응답 스키마는 공통 스키마를 상속한다.
- **이유:** 화면과 MSW가 같은 TypeScript 계약을 사용하면서 요청·응답 경계를 명확히 구분한다.
- **오류 우선순위:** 존재하지 않는 지원자와 잘못된 요청은 각각 `404`, `400`으로 먼저 처리하고, 유효한 요청에만 약 15%의 임의 실패를 적용한다.
- **상태 변환:** 단계 변경은 기존 지원자 배열을 수정하지 않고 새 배열을 저장한 뒤 메모리 상태를 교체한다.

## D-007. 초기 지원자의 단계 분포

- **결정:** 1,000명의 초기 지원자를 `서류검토 32% · 면접 12% · 처우협의 3% · 최종합격 5% · 불합격 48%`로 배치한다.
- **이유:** 모든 단계를 20%씩 배치하는 것보다 뒤 단계로 갈수록 줄고 누적 불합격이 많은 실제 채용 퍼널에 가깝다.
- **데이터 갱신:** 기존 브라우저 저장 데이터가 이전 균등 분포를 유지하지 않도록 저장 키를 버전업한다.

## D-008. 검색·필터와 페이지네이션 순서

- **결정:** mock API가 이름·직무·단계 조건을 전체 지원자에 먼저 적용하고, 필터 결과를 100건씩 페이지네이션한다.
- **이유:** 클라이언트에 로드된 페이지만 필터하면 아직 조회하지 않은 지원자가 검색에서 누락되고, 전체 검색 인덱스를 별도로 선로딩하면 페이지네이션의 이점을 없앤다.
- **클라이언트 동작:** 검색·필터가 바뀌면 진행 중인 목록 요청을 취소하고 캐시 없이 각 단계의 1페이지부터 다시 조회한다.
- **직무 조건:** 기본은 모든 직무를 체크한 전체 조회이며, 일부만 남기면 반복 `role` 파라미터의 OR로 적용한다.

## D-009. AI 제안 채택과 기각

- **채택:** 브라우저 MSW, 100건 단위 API 페이지네이션, 지원자 `id` 기반 선택 상태, 상세 패널의 명시적 단계 변경, shadcn/ui 확인 모달과 낙관적 업데이트를 채택했다. 실제 백엔드 없이 저장·실패를 재현하고 Must 흐름을 가장 작은 상태 구조로 구현할 수 있기 때문이다.
- **기각:** 드래그앤드롭 단계 이동은 중요한 변경 내용을 확인하기 어렵고, 전체 검색 인덱스 선로딩은 API 페이지네이션과 중복돼 제거했다. 최종합격 이후 불합격 전이, 커스텀 디자인 시스템과 범위 밖 ATS 기능도 요구사항과 충돌하거나 필요하지 않아 포함하지 않았다.

## D-010. 완료하지 않은 Should 항목

Must 통합 검증에서 추가 결함이 발견되지 않아 다음 항목은 선택 범위로 남겼다.

- **빠른 연속 변경 경쟁 상태 고도화:** 현재 저장 중인 지원자의 추가 변경은 차단한다. 요청 버전 관리나 큐는 실제 경쟁 문제가 확인될 때 추가한다.
- **1,000건 가상화·추가 성능 최적화:** 100건 페이지네이션과 컬럼별 스크롤을 사용하며, 브라우저에서 1,000건 검색 결과 갱신을 `744ms`에 확인했다. 측정된 병목이 생길 때 가상화를 검토한다.
- **최근 단계 변경 실행 취소:** 종료 상태와 알림 가능성을 포함한 취소 정책이 정의되지 않아 추가하지 않았다.
- **핵심 상태 로직의 추가 `node:test`:** 필터와 페이지네이션 스크립트는 유지하고 단계 저장·롤백은 브라우저에서 검증했다. 별도 단위 테스트는 Should 범위로 남겼다.
- **키보드 전용 단계 이동·상세 열람:** 기본 버튼·Sheet의 접근성 동작은 유지했지만 전용 키보드 흐름은 별도로 구현하지 않았다.
