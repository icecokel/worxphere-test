# AI 협업 기록

## [project-setup] 프로젝트 기반 설정

### 프롬프트 1

> 프로젝트 세팅한다.
>
> 1. nextjs, ts, tailwind
> 2. shadcn
> 3. msw

### AI 출력 요지

- Next.js App Router와 TypeScript, Tailwind CSS 프로젝트를 생성했다.
- shadcn/ui를 초기화하고 기본 Button 컴포넌트를 추가했다.
- MSW worker와 브라우저 초기화 파일을 구성했다.

### 리뷰 / 검증

- Next.js 기본 예제 화면과 프로젝트에 불필요한 안내 링크를 제거했다.
- 실제 백엔드가 없는 과제이므로 MSW 시작이 끝난 뒤 애플리케이션을 표시하도록 구성했다.
- MSW 자동 생성 worker는 ESLint 검사에서 제외하고 애플리케이션 코드는 `pnpm lint`로 검증했다.
- `pnpm build`로 타입 검사와 프로덕션 빌드가 통과하는지 확인했다.

## [workflow-guardrails] 기능 단위 커밋 자동화

### 프롬프트 1

> - 하나의 기능을 하나의 커밋으로 기록한다.
> - 커밋을 squash하거나 force-push로 정리하지 않는다.
> - `PROMPTS.md`에 기능별 프롬프트와 AI 출력에 대한 실제 리뷰·검증 내용을 남긴다.
>
> 이 부분을 스킬과 훅으로 만들어 자동으로 세팅되도록하자.

### AI 출력 요지

- 기능 키를 커밋과 `PROMPTS.md`에서 일치시키는 프로젝트 스킬을 추가했다.
- 기능 커밋의 메시지와 프롬프트 로그 구조를 검사하는 `commit-msg` 훅을 추가했다.
- 비 fast-forward push를 막는 `pre-push` 훅과 자동 설치 스크립트를 추가했다.

### 리뷰 / 검증

- 하나의 커밋이 의미적으로 하나의 기능인지는 훅으로 정확히 판별할 수 없어 스킬의 판단 규칙으로 남겼다.
- squash만 선별해 차단하는 것은 신뢰할 수 없어 전체 rebase를 막지 않고 스킬 규칙으로 유지했다.
- force-push 결과인 비 fast-forward 갱신은 `pre-push`에서 차단하도록 했다.
- `pnpm test:hooks`로 올바른 로그가 있는 커밋과 fast-forward push는 통과하고, 범위·로그가 없는 기능 커밋과 비 fast-forward push는 실패하는지 검증했다.
- 스킬 validator와 `pnpm lint`를 통과했으며, 설치된 훅 파일의 실행 권한과 `core.hooksPath` 설정을 확인했다.

### 프롬프트 2

> agents.md에 추가해서 일관되도록 하고

### AI 출력 요지

- 기능 단위 커밋, `feature-key`, `PROMPTS.md`, squash·force-push 규칙을 `AGENTS.md`에 추가했다.
- 스킬과 훅이 담당하는 범위를 `AGENTS.md`에서도 동일하게 명시했다.

### 리뷰 / 검증

- Next.js가 관리하는 자동 생성 블록은 수정하지 않고 프로젝트 규칙을 블록 밖에 추가했다.
- `atomic-feature-commits` 스킬, Git 훅, `AGENTS.md`의 규칙이 서로 모순되지 않는지 대조했다.

### 프롬프트 3

> 커밋

### AI 출력 요지

- 현재 변경을 논리 단위별 커밋으로 분리했다.
- 커밋 후 훅 테스트를 다시 실행해 추적된 `PROMPTS.md`가 테스트 인덱스에서 변경으로 잡히지 않는 문제를 발견했다.

### 리뷰 / 검증

- 실제 작업 트리를 수정하지 않고 대체 인덱스에 테스트용 `PROMPTS.md` blob을 등록하도록 수정했다.
- 기존 커밋을 squash하거나 수정하지 않고 별도의 수정 커밋으로 검증 과정을 보존했다.

## [screen-spec] 파이프라인 화면 설계

### 프롬프트

> 요구사항 문서를 보고 작업 전 와이어프레임을 Mermaid로 작성한다.
>
> 나인하이어 ATS를 레퍼런스로 사용하고, 작업자가 바로 이해할 수 있도록 화면 명세를 자세히 작성한다.

### AI 출력 요지

- 단계별 컬럼, 지원자 카드, 검색·필터, 상세 패널과 화면 상태를 Mermaid 와이어프레임으로 정리했다.
- 레이아웃, 표시 정보, 상호작용, 낙관적 업데이트, mock API 계약과 완료 확인 기준을 문서화했다.

### 리뷰 / 검증

- `docs/requirements.md`의 Must와 화면 명세를 대조했다.
- 나인하이어 화면의 카드형 정보 구조만 채택하고 범위 밖인 공고 관리, 평가, 메시지, 일정 기능은 제외했다.
- 요구사항에 없는 뒤로 가기, 공고 상태, 상대 시간 정보를 와이어프레임에서 제거했다.
- 저장소 검색 결과 기존 공용 UI는 `Button`만 확인되어, 나머지는 필요한 shadcn/ui 구성요소만 추가하도록 명시했다.

## [implementation-plan] Phase별 구현 계획

### 프롬프트

> 와이어프레임 기준 작업 계획서를 `docs/plan`에 작성한다.
>
> Phase와 Task로 나누고 범위가 크면 Phase별 파일로 분리한다.

### AI 출력 요지

- 전체 인덱스와 5개 Phase 실행 문서로 계획을 분리했다.
- 각 Task에 feature-key, 예상 파일, 작업 체크리스트, 검증, 커밋 경계를 명시했다.

### 리뷰 / 검증

- 요구사항, 와이어프레임, 설계 결정을 기준 문서로 고정했다.
- 모든 Must 요구사항을 담당 Task에 연결하고 Should는 Phase 5 이후로 분리했다.
- 기존 `Button` 우선 재사용, 필요한 shadcn/ui만 추가, 별도 DnD 라이브러리 미사용 원칙을 반영했다.
- Phase 간 선행조건과 기능 단위 커밋 순서가 일치하는지 대조했다.

## [coding-conventions] 코딩 컨벤션 정의

### 프롬프트

> 코딩 컨벤션 작성
>
> 1. 파일명을 케밥을 유지한다.
> 2. 배럴구조를 사용하지 않는다.
> 3. 익명 함수 사용하지 않는다.

### AI 출력 요지

- 파일명, 모듈 내보내기, 함수 작성 규칙을 `docs/coding-conventions.md`에 정의했다.
- 코딩 작업 시 항상 적용되도록 `AGENTS.md`에 동일한 핵심 규칙을 추가했다.

### 리뷰 / 검증

- 사용자가 제시한 세 가지 규칙만 문서화하고 추가 스타일 규칙은 만들지 않았다.
- 프레임워크가 요구하는 고정 파일명만 케밥 케이스 규칙의 예외로 명시했다.
- 배럴 구조와 익명 함수의 금지 범위를 직접 import와 이름 있는 콜백까지 구체화했다.

## [shadcn-default-design] shadcn 기본 디자인 규칙

### 프롬프트

> 디자인은 주요 요건이 아니므로 shadcn의 기본 디자인을 적극적으로 활용하고 스타일이 유지되도록 디자인 스킬을 생성한다.

### AI 출력 요지

- UI 구현·리뷰에 자동 적용되는 `shadcn-default-design` 프로젝트 스킬을 추가했다.
- `base-nova`, `neutral`, semantic token과 shadcn/ui 기본 variant를 우선하는 규칙을 정의했다.
- 화면 명세와 구현 계획의 시각 기준을 같은 원칙으로 정리했다.

### 리뷰 / 검증

- 기존 `shared-ui-first`는 컴포넌트 선택, 새 스킬은 시각적 일관성을 담당하도록 역할을 분리했다.
- 나인하이어 레퍼런스는 정보 구조에만 적용하고 브랜드 시각 복제는 제외했다.
- 요구사항이 없는 커스텀 팔레트, 폰트, 그림자, radius, animation을 만들지 않도록 제한했다.
- 단일 `SKILL.md`로 충분해 스크립트, 레퍼런스, UI 메타데이터는 추가하지 않았다.

## [mock-api] 지원자 조회와 단계 저장 API

### 프롬프트

> 1. feature/msw/mock-api
> 2. 작업 시작한다
> 3. 타입 스트립트로 스키마 명시한다.
> 4. 공통 스키마, 요청, 응답 스키마 명시 후 상속 받는구조

### 프롬프트 2

> 지원자 1000건 이상으로 만들어야 으로
> 몇가지 규칙으로 생성되도록 하는게 좋다.
>
> 뮤테이션이 일어날 예정이기때문에, 상태변환
> 고려해서 작업을 진행해야 한다.

### 프롬프트 3

> 일반적으로 1년이치를 잘 안본다고 판단된다.
> 기간은 30일로 고정한다.
>
> 이름이 반복해서 나올 수 있음으로,
> 이름 생성 또한 조합으로 진행한다.
> 15개의 성씨 와 15개 이상의 이름으로 조합을한다.

### AI 출력 요지

- 공통 지원자 스키마와 단계 변경 요청·응답, 목록·오류 응답 스키마를 TypeScript로 정의했다.
- 순번 ID, 15개 성씨와 20개 이름 조합, 직무·단계 순환, 최근 30일 분산 규칙으로 1,000명의 초기 지원자를 생성했다.
- GET·PATCH MSW 핸들러에 지연, 임의 실패, 요청 검증과 성공 상태 저장을 구현했다.
- 단계 변경을 기존 배열을 수정하지 않는 상태 변환 함수로 분리했다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`를 통과했다.
- Playwright 브라우저에서 GET 성공 시 1,000건과 GET 실패 시 `500` 응답을 확인했다.
- 생성된 1,000건의 ID가 모두 고유하고 순번·직무·단계·지원일 분산 규칙을 따르는지 확인했다.
- 15개 성씨와 20개 이름이 300개 조합을 만들고 지원일이 30개 날짜에 분산되는지 확인했다.
- PATCH 성공 응답과 저장 단계가 일치하고 새로고침 후에도 유지되는지 확인했다.
- PATCH 임의 실패 시 기존 저장 단계가 유지되는지 확인했다.
- 존재하지 않는 지원자의 `404`, 허용되지 않은 단계의 `400`, 손상되거나 1,000건 미만인 저장 데이터의 초기값 복구를 확인했다.

## [phase-completion-criteria] Phase 완료 기준 보강

### 프롬프트

> 모든 Phase 문서에는 목표와 완료 기준이 있어야 한다. 부족한 부분을 개선해 문서를 갱신한다.

### AI 출력 요지

- 5개 Phase 문서의 목표 아래에 결과 중심의 완료 기준 체크리스트를 추가했다.
- 완료 기준은 완료 여부 판단, 기존 Phase 검증은 실행 절차 확인으로 역할을 구분했다.

### 리뷰 / 검증

- 모든 `phase-*.md`에 `## 목표`와 `## 완료 기준`이 각각 하나씩 있는지 확인했다.
- 기존 Task와 검증 절차, Must·Should 범위, mock API 진행 상태를 변경하지 않았다.

## [pipeline-board] 읽기 전용 파이프라인 보드

### 프롬프트

> 1. feature/pipeline-board 브랜치 생성하고
> 2. 작업 시작
> 3. 목표에 완수했는지 코드레벨에서 확인
>
> 100개씩 페이지네이션 하고,
> 섹션 별로 스크롤링 만들고
> 인피니티 스크롤로 진행한다.

### AI 출력 요지

- `feature/pipeline-board` 브랜치를 만들고 `/`에 읽기 전용 파이프라인 보드를 연결했다.
- 공용 `STAGES` 순서로 5개 컬럼과 지원자 카드를 표시하고, 지원일 형식·컬럼 인원수·부분 및 전체 빈 상태를 구현했다.
- `loading`, `success`, `error` 상태와 요청 취소, 실패 후 같은 GET 요청을 실행하는 재시도를 구성했다.
- 단계별 지원자를 100개 단위로 조회하고 각 컬럼 하단에서 다음 페이지를 불러오는 독립 스크롤을 구성했다.
- 기존 `Button`을 재사용하고 shadcn/ui Card와 Skeleton만 추가했다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright에서 초기 로딩 중 5개 컬럼 스켈레톤이 표시되는지 확인했다.
- 고정 응답으로 컬럼 순서, 카드 위치와 필수 정보, `288px` 컬럼 너비, `12px` 간격, 컬럼별 인원수를 확인했다.
- GET 실패 문구와 기존 `Button` 기반 재시도 후 성공 전환을 확인했다.
- 전체 빈 상태의 모든 인원수 `0`과 `등록된 지원자가 없습니다.`, 일부 빈 컬럼의 `지원자 없음`을 구분해 확인했다.
- `375px` 화면에서 페이지 자체는 넘치지 않고 보드 영역만 가로 스크롤되는지 확인했다.
- 초기 구현에서 실제 MSW 응답의 1,000개 카드가 단계별 200개씩 표시되는지 확인했다.
- `pnpm test:pagination`으로 100개 단위 분할, 전체 인원수, 마지막 페이지 종료 조건을 확인했다.
- 추가 요구사항 적용 후 실제 MSW 응답에서 단계별 최초 100개만 표시되고 한 컬럼 하단 스크롤 시 해당 컬럼만 200개로 증가하는지 확인했다.
- 브라우저 콘솔 오류·경고가 없는지 확인했다.
- 검색·직무 필터, 상세 패널, 카드 선택·드래그·저장은 Phase 2 범위 밖이므로 포함하지 않았다.

## [applicant-stage-distribution] 초기 지원자 단계 분포 조정

### 프롬프트

> 데이터가 너무 정직하게 200개씩 끊어져 있는데 왜그럴까?
>
> 현실적인 비율 상정해봐,.
>
> api 갱신

### AI 출력 요지

- 5개 단계를 20%씩 순환 배치하던 초기 데이터 생성 규칙을 채용 퍼널 형태의 비율 배치로 변경했다.
- 1,000명을 `서류검토 320명 · 면접 120명 · 처우협의 30명 · 최종합격 50명 · 불합격 480명`으로 생성한다.
- 기존 브라우저 저장 데이터가 균등 분포를 계속 복원하지 않도록 저장 키를 버전업했다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm build`, `pnpm test:pagination`, `git diff --check`를 통과했다.
- 실제 MSW에서 단계별 `total`이 `320 · 120 · 30 · 50 · 480`이고 합계가 1,000명인지 확인했다.
- 기존 페이지네이션 관련 미커밋 변경은 수정하거나 스테이징하지 않았다.

## [candidate-filter] 지원자 이름 검색과 직무 필터

### 프롬프트

> 페이즈 3 문서 읽는다.
>
> 1. feature/search-detail 브랜치 생성
> 2. 작업 시작

### 프롬프트 2

> 목적 기준에 완수 했는가
>
> 보완 시작

### 프롬프트 3

> 검색운 페이지 네이션이 있음으로 api로 진행해야한다.
>
> 직무 UI를 체크 박스로 진행한다.

### 프롬프트 4

> 체크박스를 클릭 할 때 마다 api 호출해서 신선한 데이터 상태를 유지 해야 한다.

### 프롬프트 5

> 체크박스 기본값은 전체 체크드이다.

### AI 출력 요지

- `feature/search-detail` 브랜치를 만들고 이름 검색 입력과 로드된 데이터 기반 직무 선택을 추가했다.
- 이름 부분 검색과 직무 정확 일치 조건을 AND로 적용하고, 표시 카드와 컬럼 인원수를 같은 필터 결과에서 계산했다.
- 별도 검색 라이브러리나 디바운스 없이 `useMemo`, `Set`, 표준 배열 메서드를 사용했다.
- 기존 shadcn/ui 스타일을 유지하는 `Input`, `Select`만 추가하고 필터의 빈 결과 상태를 구현했다.
- 무쿼리 `GET /api/applicants`로 전체 지원자 검색 인덱스를 한 번 불러와 컬럼 페이지네이션에 아직 포함되지 않은 지원자도 검색되게 보완했다.
- 검색 인덱스 조회 실패 시 필터를 비활성화하고 기존 `Button`으로 재시도할 수 있게 했다.
- 전체 검색 인덱스 선로딩을 제거하고 이름·직무 조건을 단계별 GET API 쿼리로 전달하도록 보정했다.
- mock API가 전체 지원자에서 이름·직무·단계를 먼저 필터한 뒤 100건씩 페이지네이션하도록 변경했다.
- 필터 변경 시 이전 요청을 취소하고 각 컬럼을 1페이지부터 다시 조회하며, 직무 선택지는 최초 API 응답에서 누적해 유지했다.
- 단일 직무 `Select`를 shadcn/ui 체크박스로 바꾸고 선택 없음은 전체, 여러 직무 선택은 OR 조건으로 처리했다.
- 선택한 직무는 반복 `role` 쿼리 파라미터로 전달하고 mock API에서 모두 읽도록 변경했다.
- 체크 상태가 바뀔 때마다 이전 요청을 취소하고 `cache: "no-store"`로 각 컬럼의 1페이지를 다시 조회하도록 명시했다.
- 모든 직무 체크박스를 기본 선택 상태로 표시하고, 일부 해제 시 남은 직무만 API 조건으로 전달하도록 변경했다.

### 리뷰 / 검증

- `pnpm test:filter`로 한글 부분 검색, 앞뒤 공백 제거, 영문 대소문자 무시, 다중 직무 OR, 이름·직무 AND 조건과 초기화를 확인했다.
- 체크박스 전환 후 `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.
- 체크박스 재조회에 `cache: "no-store"`를 적용한 뒤 같은 검증을 다시 통과했다.
- Playwright에서 이름 `민준` 검색과 `백엔드 개발자` 필터를 함께 적용했을 때 카드와 컬럼 인원수가 함께 줄어드는지 확인했다.
- 실제 MSW 응답에서 380건이 로드된 상태로 필터가 즉시 반영되고, 결과가 없을 때 5개 컬럼과 `조건에 맞는 지원자가 없습니다.`가 유지되는지 확인했다.
- 검색과 직무를 초기화하면 단계별 전체 인원수 `320 · 120 · 30 · 50 · 480`이 복원되는지 확인했다.
- `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- 검색 인덱스 최초 조회가 임의 실패했을 때 오류 문구와 `다시 시도`가 표시되고 재시도 후 필터가 활성화되는지 확인했다.
- 최초 컬럼 페이지에 없는 `applicant-0121`, `applicant-0421`, `applicant-0721`이 이름 `김서준` 검색 결과로 각각 표시되는지 확인했다.
- 전체 인덱스 결과 카드에서도 지원자 상세 패널이 열리는지 확인했다.
- 보완 후 `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright에서 이름 `김서준` 검색이 최초 컬럼 페이지 밖의 `applicant-0121`, `applicant-0421`, `applicant-0721`을 API 응답으로 표시하는지 확인했다.
- `프론트엔드 개발자` 필터에서 불합격 컬럼의 API 결과가 총 120건이고, 최초 100건 뒤 다음 페이지 20건을 이어서 표시하는지 확인했다.
- 전체 인덱스 선로딩 요청과 클라이언트 필터 계산이 제거됐는지 코드 수준에서 확인했다.
- 통합 후 `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright에서 프론트엔드·백엔드 동시 선택 시 불합격 합계가 240건으로 OR 적용되고, 스크롤 후 2페이지까지 200건이 이어 붙는지 확인했다.
- 같은 다중 직무 조건에서 이름 `김서준`을 함께 적용해 최초 페이지 밖의 지원자가 이름·직무 AND 조건으로 표시되는지 확인했다.
- 새로고침 직후 네 직무 체크박스가 모두 선택되고, 프로덕트 매니저 해제 시 나머지 세 직무 기준으로 API 합계가 갱신되는지 확인했다.

## [candidate-detail] 지원자 상세 패널

### 프롬프트

> 페이즈 3 문서 읽는다.
>
> 1. feature/search-detail 브랜치 생성
> 2. 작업 시작

### AI 출력 요지

- 지원자 카드 선택 상태를 객체 복사본이 아닌 `id`로 보관하고, 선택한 지원자의 필수 정보를 우측 Sheet에 표시했다.
- 카드에 지원자 이름을 포함한 접근 가능한 레이블과 포커스 링을 추가했다.
- 닫기 버튼, `Escape`, 바깥 영역 선택으로 패널을 닫고 검색·필터 결과에서 선택한 지원자가 제외돼도 닫히도록 했다.
- shadcn/ui Sheet 기본 동작과 스타일을 유지하면서 데스크톱 `420px`, `640px` 미만 전체 너비만 적용했다.

### 리뷰 / 검증

- Playwright에서 `김민준`, `이민준` 카드를 각각 선택해 이름·직무·지원일·현재 단계가 카드와 일치하는지 확인했다.
- 닫기 버튼, `Escape`, 패널 바깥 영역 선택이 각각 패널을 닫는지 확인했다.
- 선택한 `김민준`이 제외되도록 이름 필터 상태를 변경했을 때 패널이 닫히는지 확인했다.
- 계산된 패널 너비가 1280px 화면에서 `420px`, 375px 화면에서 `375px`인지 확인했다.
- 패널 닫기는 선택 `id`만 초기화하고 보드 데이터와 스크롤 DOM은 유지하는지 코드 수준에서 확인했다.
- `pnpm test:filter`, `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.

## [detail-fields-requirement] 지원자 상세 최소 필드 기준

### 프롬프트

> 각 이슈별 별도의 워크트리에서 작업 진행후 main PR 작업진행한다.
> 모든 이슈 해결까지 진행하고 완료되면 PR 링크만 제공

- 대상: GitHub Issue #18 `지원자 상세 보기의 최소 표시 필드 정의`

### AI 출력 요지

- 상세 보기의 최소 표시 필드를 이름, 직무, 지원일, 현재 단계로 요구사항에 명시했다.
- 연락처, 이력서, 평가, 메모, 단계 이력은 상세 보기 범위 밖으로 유지했다.
- 화면 명세의 완료 확인 기준을 최소 필드와 범위 밖 정보 기준에 맞췄다.

### 리뷰 / 검증

- 기존 화면 명세와 상세 패널 구현이 네 최소 필드를 이미 표시하고 범위 밖 필드를 만들지 않는지 대조했다.
- `src/components/pipeline-board.tsx`에서 이름, 직무, 지원일과 현재 단계의 상세 패널 렌더링을 확인했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- 공유 `node_modules` 심볼릭 링크로 인한 Turbopack 외부 경로 오류는 워크트리 로컬 의존성 설치 후 재검증해 해소했다.
- `git diff --check`를 통과했다.

## [stage-move] 카드 단계 이동과 실패 롤백

### 프롬프트

> 문서 기준 작업 시작

### AI 출력 요지

- HTML Drag and Drop API로 지원자 ID를 전달하고 다른 단계 컬럼만 유효한 drop target으로 처리했다.
- 드롭 즉시 같은 보드 상태에서 카드, 컬럼 인원수, 상세 패널 단계를 갱신하고 `PATCH /api/applicants/:id/stage`를 한 번 호출했다.
- 저장 중인 지원자 ID만 `Set`으로 관리해 카드에 투명도와 `저장 중…`을 표시하고 추가 드래그를 막았다.
- PATCH 실패 시 이동 전 단계와 배열 위치를 복원하고 기존 `Card`, `Button`으로 닫기와 자동 소멸이 가능한 `role="alert"` 알림을 표시했다.
- 드래그와 짧은 선택을 구분하고 드래그 중 ID는 동기 ref에 보관해 빠른 `dragover`에서도 drop 허용이 누락되지 않게 했다.
- 별도 DnD 라이브러리, 새 의존성, 범용 상태 추상화는 추가하지 않았다.

### 리뷰 / 검증

- Playwright에서 5개 단계의 서로 다른 방향 20개 조합을 이동하고 각 PATCH가 `200`으로 완료되는지 확인했다. 1280px에서 화면 밖인 양 끝 컬럼 조합은 1920px에서 확인했다.
- 같은 컬럼 drop 전후 네트워크 기록에 PATCH가 추가되지 않고 카드 선택 패널도 열리지 않는지 확인했다.
- 저장 중 카드가 `aria-busy="true"`, `draggable=false`, `저장 중…` 상태이며 두 번째 대상 컬럼으로 이동되지 않고, 완료 후 다시 드래그 가능해지는지 확인했다.
- 성공한 PATCH 후 새로고침해 원본 컬럼 `320 → 319`, 대상 컬럼 `120 → 121`과 이동한 카드 단계가 유지되는지 확인했다.
- 이름 `김민준` 검색 중 이동해 카드와 두 컬럼 인원수가 함께 갱신되는지 확인했다.
- 런타임에서 임의 실패를 강제로 발생시켜 카드와 인원수의 원래 위치 복원, 실패 알림 표시, 재이동 가능 상태를 확인하고 강제 설정을 복원했다.
- 저장 중 이동 카드의 상세 패널이 대상 단계 `면접`을 즉시 표시하고, 강제 실패 후 원래 단계 `서류검토`로 복구되는지 확인했다.
- 실패 알림의 직접 닫기 전후 개수가 `1 → 0`이 되고, 직접 닫지 않은 알림은 일정 시간 뒤 사라지는지 확인했다.
- 드래그 중 대상 컬럼에 `border-primary`, `bg-secondary`가 적용되는지 확인했다.
- `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

### 프롬프트 2

> 지원자 이동을 DND 가 아닌 상세해서 상태 변경으로 바꾸는 흐름으로 바꾼다.
>
> - 지원자에게는 중요한 결정이고, 결정에 따라 지원자에게 알림이 가는 복잡성을 고려해야한다.
>
> 단계별로 하나씩 넘어가야 한다.
>
> 불합격은 언제나 할 수 있다.
>
> 서류검토 -> 면접 -> 처우협의 -> 합격
>
> ---
>
> 불합격
>
> 1. 섹션을 나눠서 상태를 변경 할 수 있게한다.
> 2. 단계를 선택하는 것이 아닌 현재 상태에 맞춰 세팅되도록

### AI 출력 요지

- 카드 드래그앤드롭과 단계 선택 UI를 제거하고, 상세 패널에서만 상태를 변경하도록 전환했다.
- 상세 패널을 `다음 채용 단계`와 `불합격 처리` 섹션으로 나눴다.
- 정규 전이는 `서류검토 → 면접 → 처우협의 → 최종합격`의 바로 다음 단계만 제공하고, 불합격은 이미 불합격이 아닌 모든 상태에서 별도 처리할 수 있게 했다.
- 변경 전 지원자 이름, 이전·대상 단계, 알림 발송 가능성을 확인받고, 기존 낙관적 저장·실패 롤백·오류 알림을 유지했다.
- 실제 알림 발송은 범위 밖으로 두고, 변경 영향 가능성만 고지했다.

### 리뷰 / 검증

- Playwright에서 한 지원자를 `서류검토 → 면접 → 처우협의 → 최종합격` 순서로 변경하고 각 PATCH 성공을 확인했다.
- 최종합격 상태에서도 별도 `불합격 처리` 액션이 제공되는지 확인했고, PATCH 실패 시 최종합격으로 롤백된 뒤 재시도로 불합격 저장과 새로고침 후 유지가 되는지 확인했다.
- 확인창을 취소했을 때 화면 변경과 PATCH 요청이 생기지 않는지, 이미 불합격인 지원자에게 정규 진행 단계가 제공되지 않는지 확인했다.
- `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

### 프롬프트 3

> [다음 단계로 변경]
> 문구가 아닌 현재 스텝에서 다음 스텝으로 갈 때 문구로 진행하는 것이 좋다고 생각.
> eg )
> [면접 단계로 변경]
> [처우 협의 단계로 변경]

### AI 출력 요지

- 정규 진행 버튼에 현재 상태에서 계산된 대상 단계명을 표시하도록 변경했다. 예: `면접 단계로 변경`, `처우협의 단계로 변경`.
- 단계명은 기존 데이터 계약의 `처우협의` 표기를 유지했다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

### 프롬프트 4

> 최종 합격은 불합격이 될 수 없다

### AI 출력 요지

- 불합격 전이 조건을 정규 진행 중인 `서류검토`, `면접`, `처우협의`로 제한했다.
- 최종합격 상세 패널에서는 불합격 버튼을 비활성화하고 종료 상태임을 안내한다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

### 프롬프트 5

> 1. 최종합격에서는 불합격 처리 UI 를 표시하지 않는다.
> 2. api에서도 최종합격의 상태의 지원자 상태 변경을 불합격으로 시도하는경우 400 에러를 떨어 트린다.

### AI 출력 요지

- 최종합격 상세 패널에서는 `불합격 처리` 섹션을 렌더링하지 않는다.
- mock API는 최종합격 지원자를 불합격으로 변경하려는 PATCH 요청을 임의 실패 처리 전에 `400`으로 거부한다.

### 리뷰 / 검증

- Playwright에서 최종합격 지원자 상세 패널에 `불합격 처리` UI가 없는 것을 확인했다.
- `PATCH /api/applicants/applicant-0471/stage`에 `{ "stage": "불합격" }`을 요청해 `400`과 오류 메시지를 확인했다.
- `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

### 프롬프트 6

> 컴펌도 모달로 만든다 샤드씨엔 참고해서 만들고

### AI 출력 요지

- 브라우저 기본 확인창을 shadcn/ui `AlertDialog`로 교체했다.
- 정규 단계 변경과 불합격 처리가 같은 확인 모달을 사용하며, 불합격 확정 버튼에만 destructive variant를 적용했다.
- 모달의 취소는 대상 단계 상태만 지우고, 확정은 기존 낙관적 저장과 롤백 경로를 호출한다.
- 상세 패널 위에서도 모달을 조작할 수 있도록 `AlertDialog`의 레이어를 패널보다 높게 설정했다.

### 리뷰 / 검증

- Playwright에서 정규 단계 변경과 불합격 처리 모달에 지원자·현재·대상 단계·알림 가능성이 표시되고 취소가 모달을 닫는지 확인했다.
- 정규 단계 변경을 확정해 카드의 단계가 `서류검토`에서 `면접`으로 갱신되는지 확인했다.
- `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

## [release-check] 통합 검증과 제출 문서

### 프롬프트 1

> 페이즈 5 문서 읽어봐.

### 프롬프트 2

> 1. feature/`validation` 브랜치 생성
> 2. 작업 시작

### AI 출력 요지

- `feature/validation` 브랜치를 만들고 Phase 5의 Must 범위만 통합 검증했다.
- 자동 검증과 실제 브라우저의 로딩·성공·오류·빈 상태, 검색·필터, 상세 패널, 단계 저장·롤백과 반응형 동작을 확인했다.
- README의 실행·검증 명령과 mock API 설명, DECISIONS의 AI 제안 검토와 미완료 Should 기록을 실제 구현에 맞게 갱신했다.
- 새 기능이나 라이브러리는 추가하지 않았다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm build`, `pnpm test:hooks`가 첫 실행에서 모두 통과했고 빌드 생성물이 추적 파일을 변경하지 않았다.
- 최초 진입에서 5개 컬럼의 전체 인원수 `320 · 120 · 30 · 50 · 480`과 1,000건 데이터를 확인했다. 첫 페이지에는 단계별 최대 100건씩 총 380개 카드가 표시됐다.
- GET의 실제 임의 `500` 실패에서 `지원자를 불러오지 못했습니다.`와 `다시 시도`를 확인하고, 재시도 `200` 후 해당 컬럼이 복구되는지 확인했다.
- 한글 `김민준` 부분 검색과 프론트엔드 직무 필터의 동시 적용을 확인했다. 검증 세션에서 첫 지원자 이름만 `Alex Kim`으로 임시 변경해 `ALEX` 검색이 대소문자와 무관하게 1건을 찾는지 확인한 뒤 저장소를 초기화했다.
- 1,000건 데이터에서 검색 입력부터 5개 컬럼 결과 완료까지 `744ms`로 측정했으며 의도된 `200~800ms` API 지연 외 체감 지연은 없었다.
- 빈 GET 응답과 한 컬럼만 있는 응답을 브라우저에 임시 주입해 `등록된 지원자가 없습니다.`, `조건에 맞는 지원자가 없습니다.`, `지원자 없음`을 구분해 확인했다. 초기 GET에는 2초 지연을 임시 주입해 비활성 검색 입력과 5개 로딩 컬럼을 확인했다.
- 카드 선택 시 이름·직무·지원일·현재 단계, `다음 채용 단계`와 `불합격 처리` 섹션을 확인했다. 확인 모달 취소 전후 요청 번호가 `57`로 같아 PATCH가 발생하지 않았고 카드·상세 값도 유지됐다.
- `applicant-0001`의 `서류검토 → 면접` PATCH `200` 후 새로고침해 면접 단계가 유지되는지 확인했다. PATCH 호출을 1.5초 늦춘 검증에서는 100ms 뒤 인원수 `면접 0 · 처우협의 2`, 상세 단계 `처우협의`, 비활성 `변경 중…` 버튼 2개가 먼저 표시돼 낙관적 갱신을 확인했다.
- PATCH를 `500`으로 강제한 검증에서 카드·인원수·상세 단계가 원래 `서류검토`로 복구되고 `role="alert"` 실패 알림이 표시되는지 확인했다.
- 최종합격 상세 패널에 불합격 처리 UI가 없고 다음 단계가 없음을 확인했다. 같은 지원자에 `{ "stage": "불합격" }`을 직접 요청해 `400`과 `최종합격한 지원자는 불합격 처리할 수 없습니다.` 응답을 확인했다.
- `390px` 화면에서 보드 `clientWidth 342px · scrollWidth 1488px`의 가로 스크롤과 상세 패널 `390px` 전체 너비를 확인했다.
- 브라우저 저장소를 삭제한 뒤 초기 인원수로 복구했고, 검증용 브라우저 세션을 닫고 `.playwright-cli` 생성물을 휴지통으로 이동했다.

## [should-plan] Phase 6 Should 개선 계획

### 프롬프트

> phase 6문서 만들어줘

### AI 출력 요지

- 요구사항 문서의 Should 다섯 항목을 Phase 6의 독립 Task와 feature-key로 연결했다.
- 상태 로직 테스트, 빠른 연속 변경, 키보드 흐름, 실행 취소, 1,000건 성능 측정 순서로 작업·완료 기준·검증·커밋 경계를 정의했다.
- 종료 상태와 이전 단계 변경에 관한 Must를 유지하도록 실행 취소의 최소 범위를 명시했다.
- 성능은 먼저 측정하고 병목이 확인될 때만 현재 의존성으로 개선하도록 제한했다.

### 리뷰 / 검증

- `docs/requirements.md`의 Should 다섯 항목이 Phase 6 Task에 각각 한 번씩 연결되는지 대조했다.
- Phase 5의 미구현 Should 목록과 `DECISIONS.md`의 보류 사유를 변경하지 않고 후속 계획으로 연결했다.
- 새 기능, 라이브러리와 범위 밖 ATS 기능을 계획에 추가하지 않았다.
- 각 구현 항목이 별도 feature-key와 커밋으로 분리되는지 확인했다.

## [stage-code] 채용 단계 영문 전송 코드

### 프롬프트

> 상태 값을 이넘화해서, 한글로 송부하지 않도록개선

### AI 출력 요지

- 채용 단계를 영문 문자열 코드 상수로 정의하고 한글 단계명은 화면 라벨 매핑으로 분리했다.
- 목록 조회 파라미터, 단계 변경 요청·응답과 브라우저 저장소가 영문 코드를 사용하도록 변경했다.
- 저장 스키마 변경에 맞춰 브라우저 저장 키를 `v3`로 올리고 API 계약 문서와 검증 스크립트를 갱신했다.

### 리뷰 / 검증

- `pnpm test:stage-code`, `pnpm test:filter`, `pnpm test:pagination`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.
- 브라우저에서 컬럼·카드·상세 패널·확인 모달의 단계명이 한글로 유지되는지 확인했다.
- GET 요청의 단계 파라미터가 `DOCUMENT_REVIEW`, `INTERVIEW`, `COMPENSATION_NEGOTIATION`, `HIRED`, `REJECTED`로 전송되는지 확인했다.
- `서류검토 → 면접` 변경을 확정해 PATCH 본문이 `{ "stage": "INTERVIEW" }`이고 응답이 `200`인지 확인했다.

## [role-order] 직무 필터 순서 고정

### 프롬프트

> 직무 순서가 일관되지 않는다.
> 유저가 일관된 경험을 할 수 있도록 순서를 일관되게 변경한다.

### AI 출력 요지

- API 응답 완료 순서대로 누적되던 직무 목록을 고정된 직무 순서로 정렬했다.
- mock 데이터와 화면이 같은 직무 목록을 사용하도록 공통 상수로 통합했다.
- 정의되지 않은 직무는 누락하지 않고 고정 직무 뒤에 유지했다.

### 리뷰 / 검증

- `pnpm test:filter`, `pnpm lint`, `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.
- Playwright에서 임의 API 지연이 적용된 화면을 3회 새로고침해 `프론트엔드 개발자 → 백엔드 개발자 → 프로덕트 디자이너 → 프로덕트 매니저` 순서가 유지되는지 확인했다.

## [partial-load-error] 부분 조회 실패 상태

### 프롬프트 1

> 동의한다 수정해줘

### 프롬프트 2

> 자동 재실행 보다 UI로 접근하는게 맞다고 판단한다 수정해줘

### AI 출력 요지

- 조회에 실패한 컬럼에 이미 이동된 카드가 있으면 카드를 유지하고 작은 부분 오류 행과 수동 `다시 시도` 버튼을 표시했다.
- 첫 페이지 조회 결과를 알 수 없는 동안 컬럼 인원수에 `+`를 표시하고, 추가 페이지 실패는 별도 문구로 구분했다.
- 수동 재시도 중에도 기존 카드를 보존하고 성공한 첫 페이지 응답에서 전체 목록과 정확한 인원수로 교체했다.
- 자동 재시도와 새 UI 컴포넌트는 추가하지 않았다.

### 리뷰 / 검증

- `node scripts/check-partial-load-error.mjs`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:stage-code`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright에서 면접 첫 페이지 조회를 실패시킨 뒤 서류검토 지원자를 면접으로 이동해 카드, `1+`, `일부 지원자를 불러오지 못했습니다.`와 `다시 시도`가 함께 표시되는지 확인했다.
- `다시 시도`를 누른 직후에도 이동된 카드와 `1+`가 유지되고 오류 행만 사라지는지 확인했다.
- 재조회 성공 후 면접 인원수가 정확한 `121`로 바뀌고 기존 카드와 전체 조회 목록이 함께 표시되는지 확인했다.

## [stage-move-race] 빠른 연속 단계 변경 중복 요청 차단

### 프롬프트

> 1. feature/`pipeline-board-enhancements` 브랜치 생성
> 2. 검증 작업 말고, 구현 작업만 선진행

### AI 출력 요지

- 단계 저장 함수에 지원자별 동기 잠금을 추가해 React 상태 반영 전의 연속 확정도 두 번째 PATCH를 시작하지 않도록 했다.
- 기존 저장 중 UI 상태와 성공·실패 후 잠금 해제 흐름은 유지했다.
- 요청 큐나 범용 동시성 계층은 추가하지 않았다.

### 리뷰 / 검증

- 단계 변경의 확정부터 PATCH 호출과 잠금 해제까지 코드 흐름과 변경 diff를 검토했다.
- 사용자 요청에 따라 자동·브라우저 검증은 구현 선행 후로 보류했다.

### 추가 리뷰 / 검증

- 후속 브라우저 검증에서 같은 변경을 동기 확정 2회 실행했을 때 PATCH가 1회만 발생하는지 확인했다.
- PATCH 실패 시 원래 단계와 컬럼 인원수가 복구되고 오류 알림이 표시되는지 확인했다.
- 별도 PATCH 성공 경로에서 즉시 UI 반영과 새로고침 후 저장 상태 유지를 확인했다.
- 실패 뒤 재시도까지 이어지는 잠금 해제 시나리오는 완료 근거가 없어 통과로 기록하지 않았다.

## [stage-undo] 최근 단계 변경 실행 취소

### 프롬프트

> 1. feature/`pipeline-board-enhancements` 브랜치 생성
> 2. 검증 작업 말고, 구현 작업만 선진행

### AI 출력 요지

- 마지막으로 성공한 비종료 단계 변경 한 건을 현재 페이지 상태에만 보관하고 기존 상태 메시지 영역에 `실행 취소`를 표시했다.
- 기존 확인 모달과 PATCH를 재사용해 이전 단계로 낙관적으로 복구하며, 저장 실패 시 변경 후 단계와 배열 위치로 롤백하도록 했다.
- 실행 취소 완료, 사용자 닫기, 다음 성공 변경에서 직전 기록을 제거하고 최종합격·불합격 변경은 기록하지 않도록 했다.
- 여러 단계 이력, 영구 저장과 다중 실행 취소는 추가하지 않았다.

### 리뷰 / 검증

- 단계 변경 성공·실패, 실행 취소 성공·실패와 동시 저장 잠금의 코드 흐름 및 변경 diff를 검토했다.
- 사용자 요청에 따라 자동·브라우저 검증은 구현 선행 후로 보류했다.

### 추가 리뷰 / 검증

- 후속 브라우저 검증에서 `UNDO-01`부터 `UNDO-07`까지 모두 통과했다.
- 실행 취소 확인·취소, 성공 저장과 새로고침 유지, 실패 롤백, 기록 닫기·최신 한 건 유지, 종료 상태 제외를 확인했다.

## [stage-undo-requirement] 단계 실행 취소 예외 기준

### 프롬프트

> 각 이슈별 별도의 워크트리에서 작업 진행후 main PR 작업진행한다.
> 모든 이슈 해결까지 진행하고 완료되면 PR 링크만 제공

- 대상: GitHub Issue #20 `요구사항의 단계 이동 실행 취소 예외 규칙 명확화`

### AI 출력 요지

- 일반적인 이전 단계 변경은 계속 금지하고 직전 비종료 단계 변경 실행 취소만 예외로 명시했다.
- 실행 취소 범위를 현재 페이지의 마지막 성공 변경 1건·1회로 제한하고 종료 단계 변경, 여러 단계 이력, 영구 저장과 다중 실행 취소를 제외했다.
- 요구사항, 화면 명세와 Phase 6 계획·추적표를 같은 규칙으로 맞췄다.

### 리뷰 / 검증

- 기존 `UNDO-01`~`UNDO-07` 검증 범위와 현재 구현의 실행 취소 기록·종료 상태 제외 조건이 확정 기준과 일치하는지 대조했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- 공유 `node_modules` 심볼릭 링크로 인한 Turbopack 외부 경로 오류는 워크트리 로컬 의존성 설치 후 재검증해 해소했다.
- `git diff --check`를 통과했다.

## [phase-6-test-cases] Phase 6 검증 TC

### 프롬프트

> 검증 작업 TC 작성

### AI 출력 요지

- Phase 6 계획 문서에 단계 상태 로직, 빠른 연속 변경, 키보드 전용 흐름, 실행 취소, 1,000건 성능과 전체 회귀 TC를 추가했다.
- 각 TC에 독립적으로 재현할 절차와 판정 가능한 기대 결과를 정의했다.
- PATCH 횟수·응답·화면·저장 상태를 함께 기록하고 임시 검증 상태를 정리하도록 공통 준비 조건을 명시했다.

### 리뷰 / 검증

- Phase 6의 다섯 Task와 전체 회귀 항목이 TC에 빠짐없이 연결되는지 문서 대조했다.
- 종료 상태 규칙, 한 번만 실행 취소, 실패 롤백과 병목 확인 후 최소 개선 원칙이 기대 결과에 유지되는지 검토했다.
- 이번 작업은 TC 작성 범위이므로 자동·브라우저 검증은 실행하지 않았다.

## [stage-move-test] 단계 이동과 롤백 상태 검증

### 프롬프트

> node:test 진행해봐.

### AI 출력 요지

- 이동·롤백 순수 상태 로직을 `src/lib/pipeline-board-state.ts`로 분리했다.
- 카드 배열 위치, 단계, 원본·대상 인원수와 입력 상태 불변성을 검증하는 `node:test` 4개를 추가했다.
- `pnpm test:stage-move` 스크립트를 추가했다.

### 리뷰 / 검증

- `pnpm test:stage-move` 통과: 4개 테스트, 실패 0건.
- 사용자 요청 범위에 따라 `pnpm lint`와 `pnpm build`는 아직 실행하지 않았다.

## [stage-move-build] Node 테스트용 TS import 빌드 호환

### 프롬프트 1

> 빌드 테스트

### 프롬프트 2

> 원인 분석

### 프롬프트 3

> 수정해줘

### AI 출력 요지

- Node ESM이 요구하는 명시적 `.ts` import를 유지하면서 Next.js TypeScript 검사도 허용하도록 `allowImportingTsExtensions`를 활성화했다.
- 별도 빌드 설정이나 테스트 러너 변경은 추가하지 않았다.

### 리뷰 / 검증

- `pnpm test:stage-move` 통과: 4개 테스트, 실패 0건.
- `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

## [applicant-uuid] 지원자 UUID 적용

### 프롬프트

> UUID로 변경하자.

### AI 출력 요지

- mock 지원자 ID 생성을 순차 문자열에서 브라우저 기본 `crypto.randomUUID()`로 변경했다.
- 기존 순차 ID 저장 데이터가 다시 사용되지 않도록 브라우저 저장 키를 `v4`로 갱신했다.
- 별도 UUID 라이브러리와 해시 변환 로직은 추가하지 않았다.

### 리뷰 / 검증

- `pnpm exec eslint src/mocks/handlers.ts`를 통과했다.
- ID 참조 지점을 검색해 API 경로와 화면 선택 로직이 문자열 ID를 그대로 지원하는지 확인했다.

## [keyboard-flow] 좌우 방향키 단계 이동

### 프롬프트

> 좌우 키보드로 단계 이동이 가능해야한다.
>
> 3번 진행해줘

### AI 출력 요지

- 포커스된 지원자 카드에서 `ArrowLeft`와 `ArrowRight`로 인접 정규 단계 변경 확인 모달을 열도록 했다.
- 기존 확인 모달, PATCH, 낙관적 업데이트와 실패 롤백을 그대로 재사용했다.
- 성공과 실패 후 이동된 카드로 포커스를 복구하고 최종합격·불합격에서는 방향키 이동을 막았다.
- 카드에 `aria-keyshortcuts`를 추가하고 새 UI 컴포넌트나 의존성은 추가하지 않았다.

### 리뷰 / 검증

- `pnpm test:keyboard`, `pnpm lint`를 통과했다.
- 브라우저에서 `면접 → 서류검토`의 `ArrowLeft` 확인·성공 이동과 이동 카드 포커스 복귀를 확인했다.
- 같은 카드의 `ArrowRight` 확인 모달과 임의 PATCH 실패 시 서류검토 롤백·포커스 복귀를 확인했다.
- 검증 후 브라우저 저장 상태와 Playwright 생성물을 정리했다.

### 수정 전 추가 리뷰 / 검증

- 후속 브라우저 검증에서 `KEY-01`부터 `KEY-04`까지 통과했다.
- 방향키 단계 이동 성공 후 카드 포커스 복귀와 종료 상태의 방향키 이동·PATCH 차단을 확인했다.
- `KEY-05`에서는 단계 변경 성공 뒤 Sheet를 닫았을 때 포커스가 카드가 아닌 `body`에 남아 실패했다.
- 방향키 PATCH 실패에서는 단계·인원수 롤백과 오류 알림은 통과했지만 포커스가 카드가 아닌 `body`에 남아 실패했다.
- 이 결과는 `keyboard-focus` 정정 전 구현에 대한 기록이며, 이후 방향키 단계 이동 경로가 제거됐다.

## [keyboard-focus] 방향키 포커스 이동 정정

### 프롬프트

> 방향키는 포커스 이동만 진행이다.
>
> 키입력 동작과, 레이아웃 빠르게 수정

### AI 출력 요지

- `ArrowLeft`와 `ArrowRight`가 인접 컬럼의 같은 순번 카드로 포커스만 옮기도록 변경하고 확인 모달과 PATCH 경로를 제거했다.
- 단계 변경은 다시 다음 정규 단계와 불합격 처리만 허용하고, 최종합격·불합격 컬럼 사이도 방향키로 탐색할 수 있게 했다.
- 단계 변경 확정 후 포커스를 상세 패널 안에 유지하고 패널 종료 후 선택했던 카드로 복구했다.

### 리뷰 / 검증

- `pnpm test:keyboard`, 대상 ESLint, `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.
- 데스크톱 브라우저에서 서류검토·면접 및 최종합격·불합격 사이의 양방향 포커스 이동과 확인 모달 미생성을 확인했다.
- 단계 변경 확정 후 상세 패널의 닫기 버튼, 패널 종료 후 이동된 카드로 포커스가 복구되는지 확인했다.

## [desktop-status-layout] 데스크톱 상태 알림 배치

### 프롬프트

> 모바일은 괜찮으나 데스크톱 기준은 통과해야한다.
>
> 키입력 동작과, 레이아웃 빠르게 수정

### AI 출력 요지

- 상세 패널이 열린 동안 단계 변경 상태 알림을 패널 안에서 렌더링해 접근성 트리에서 숨겨지지 않도록 했다.
- 데스크톱에서는 `420px` 상세 패널 왼쪽에 `24px` 간격과 `384px` 너비로 배치하고, 패널이 닫힌 기존 위치는 유지했다.
- 새 UI 컴포넌트나 모바일 전용 레이아웃은 추가하지 않았다.

### 리뷰 / 검증

- 대상 ESLint, `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.
- `1440×900`과 `1024×768`에서 상태 알림이 상세 패널과 겹치지 않고 화면 안에 표시되는지 확인했다.
- 상세 패널이 열린 상태에서 `실행 취소` 버튼으로 확인 모달을 열고 `Escape`로 닫을 수 있는지 확인했다.

## [husky-pre-push] Husky pre-push 검증

### 프롬프트

> 허스키 세팅하자.
>
> 프리 푸시 에
>
> 빌드 테스트등 간단한 테스트 진행되도록해줘

### AI 출력 요지

- Husky 9를 개발 의존성에 추가하고 기존 `.githooks`를 Husky의 사용자 지정 훅 디렉터리로 재사용했다.
- `pre-push`에서 비 fast-forward 검사를 먼저 수행한 뒤 lint, 기존 Node 테스트 5개와 프로덕션 빌드를 실행하도록 했다.
- 훅 테스트가 Husky 래퍼를 거쳐 실행되도록 변경하고 기존 설치 스크립트를 제거했다.
- README와 AGENTS.md의 훅 안내를 Husky 구성에 맞게 갱신했다.

### 리뷰 / 검증

- `pnpm run prepare`를 실행하고 `core.hooksPath`가 `.githooks/_`로 설정되는지 확인했다.
- `pnpm test:hooks`를 통과했으며, 이 과정에서 commit-msg·fast-forward 보호와 `pnpm test:pre-push` 전체 흐름을 검증했다.
- `pnpm test:pre-push`에 포함된 lint, 필터·키보드·페이지네이션·단계 코드·단계 이동 테스트와 `pnpm build`가 모두 통과했다.
- `git diff --check`를 통과했다.

## [phase-6-documentation-sync] Phase 6 기록 최신화

### 프롬프트 1

> PROMPTS.md와, DECISIONS.md가 잘쓰였나 확인
>
> 확고하게 갱신가능한 부분만 갱신

### 프롬프트 2

> phase 6가 머지되었다 확고하게 갱신할 수 있는게 존재하니까
> 구현 기준 테스트 기준으로 문서 두문서 갱신

### AI 출력 요지

- 현재 코드와 검증 기록으로 확인되는 Husky pre-push, Phase 6 구현 상태, UUID와 저장 키 정보를 `DECISIONS.md`에 반영했다.
- 후속 브라우저 검증에서 확인된 경쟁 상태, 실행 취소와 키보드 결과를 기존 `PROMPTS.md` 기록에 추가했다.
- PR #6의 `main` 머지, 방향키 포커스 전용 정책, 포커스 복구와 데스크톱 상태 알림 결과를 확정 기록으로 갱신했다.
- 수정 전 키보드 실패 기록을 원래 기능 섹션으로 이동해 이력을 보존하고, 완료되지 않은 성능·잠금 해제 재시도 검증은 통과로 기록하지 않았다.

### 리뷰 / 검증

- `package.json`, `.githooks`, mock 저장소 키, 단계 이동 구현과 기존 검증 기록을 문서 내용과 대조했다.
- 확인되지 않은 시나리오는 통과로 기록하지 않았고 소스 코드는 변경하지 않았다.
- `main`의 머지 커밋과 Phase 6 커밋, 방향키 포커스·동기 잠금·실행 취소·상태 알림 구현을 검색해 문서와 대조했다.
- 머지 전 `pnpm test:pre-push`에서 lint, Node 테스트 5종과 프로덕션 빌드가 통과한 기록을 반영했다.

## [phase-6-plan-sync] Phase 6 완료 상태와 검증 TC 동기화

### 프롬프트 1

> 9번은 바로 갱신이 가능해보인다 판단하라.

### 프롬프트 2

> 갱신하고 커밋해

### AI 출력 요지

- 구현 커밋과 기존 검증 기록을 기준으로 Phase 6의 단계 상태 로직, 빠른 연속 변경, 키보드 흐름과 실행 취소 완료 상태를 계획 문서에 반영했다.
- 방향키는 인접 컬럼 카드로 포커스만 이동하고 확인 모달과 PATCH를 만들지 않으며 최종합격·불합격 컬럼도 탐색할 수 있도록 최신 TC를 추가했다.
- `1024×768`과 `1440×900`에서 상세 패널과 상태 알림의 비중첩, 실행 취소 접근 가능 여부를 판정하는 데스크톱 TC를 추가했다.
- 동시에 진행 중인 Issue #8의 성능 상태와 TC 변경은 이 커밋에서 제외하고, 아직 한 번에 완료되지 않은 전체 브라우저 회귀·정리는 미완료로 유지했다.

### 리뷰 / 검증

- 단계 상태 테스트와 빌드 보정, 동기 잠금, 키보드 포커스 정정, 실행 취소와 데스크톱 알림 배치의 구현 커밋을 계획 항목과 대조했다.
- 기존 기록에서 `pnpm test:stage-move`, `pnpm test:keyboard`, lint, build와 pre-push 검증 통과를 확인했다.
- 후속 브라우저 기록의 `UNDO-01`부터 `UNDO-07`, 방향키 포커스 전용 동작, 두 데스크톱 화면 크기의 상태 알림 결과를 TC와 대조했다.
- Issue #7의 재시도 잠금 해제 완료 상태를 반영하고, Issue #8 관련 변경을 제외한 #9 범위가 `DECISIONS.md`의 D-010과 모순되지 않는지 확인했다.
- 수정 전 키보드 실패 기록은 삭제하거나 최신 결과로 바꾸지 않고 후속 정정 기록과 분리해 유지했다.
- `git diff --check`와 스테이징된 문서 diff 검사를 통과했다.

## [pipeline-performance] 1,000건 독립 성능 검증

### 프롬프트 1

> 1. 8번 이슈 작업 계획 작성

### 프롬프트 2

> 시나리오 문서 작성

### 프롬프트 3

> e2e로 5000번 포트 별도로 열어서 테스트 진행

### 프롬프트 4

> 35555번 으로 다시 진행해줘

### 프롬프트 5

> 커밋하고
>
> 이슈에 상세히 내용 기재후 클로즈

### AI 출력 요지

- Issue #8의 이름 검색, 다중 직무 필터, 컬럼 연속 스크롤과 로드 중 응답성을 `PERF-01`부터 `PERF-04`까지의 독립 시나리오로 작성했다.
- 두 데스크톱 화면 크기에서 실제 API 시간, 마지막 응답 뒤 클라이언트 반영 시간과 Long Task를 분리해 3회씩 기록하도록 했다.
- 현재 데이터 생성 규칙에 따른 검색·직무 결과와 컬럼별 페이지 증가량을 기대값으로 고정했다.
- 병목이 재현되지 않으면 코드 변경과 가상화를 생략하고, 재현될 때만 현재 의존성으로 최소 개선하도록 제한했다.
- macOS AirPlay Receiver가 점유한 `5000` 대신 사용자 지정 `35555`에서 프로덕션 서버를 실행하고 Playwright E2E를 수행했다.
- 두 화면 크기의 이름 검색, 다중 직무 필터, 컬럼별 연속 스크롤, 동시 스크롤과 로드 중 검색 입력을 성공 기준 3회씩 측정했다.
- `pipeline-performance` 관련 문서만 기존 미커밋 변경과 분리해 커밋하고, 커밋 뒤 Issue #8에 측정·판정·회귀 결과를 기록해 종료하도록 준비했다.

### 리뷰 / 검증

- GitHub Issue #8, `docs/requirements.md`의 Should·API 지연 조건과 Phase 6의 `pipeline-performance` 완료 조건을 시나리오에 연결했다.
- 검색·필터 적용 순서, 100건 페이지네이션, 요청 취소와 컬럼별 독립 로드 경로를 현재 코드와 대조했다.
- 현재 데이터 생성 규칙으로 `김민준`의 단계별 `2 · 0 · 0 · 0 · 2`, 프론트엔드·백엔드 필터의 `160 · 60 · 16 · 24 · 240` 기대값을 다시 계산해 확인했다.
- 시나리오 문서의 연결 대상이 존재하는지 확인하고 `git diff --check`를 통과했다.
- 시나리오 작성 시점에는 성능 측정을 실행하지 않아 측정값과 완료 체크를 통과로 기록하지 않았다.

### 추가 리뷰 / 검증

- 커밋 `d0a9b97`, HeadlessChrome `151.0.0.0`, macOS `26.5.1`, 논리 CPU `14`, 메모리 `32GB`, CPU·네트워크 제한 없음 조건에서 측정했다.
- 이름 검색의 클라이언트 반영은 `43.8~51.0ms`, 다중 직무 필터는 `50.5~82.2ms`, 추가 페이지는 회차별 최대 `36.5~54.8ms`였고 모든 구간의 Long Task가 `0건`인지 확인했다.
- `PERF-03`에서 컬럼별 `100 → 200 → 300 → 320`, `100 → 120`, `30`, `50`, `100 → 200 → 300 → 400 → 480`과 최종 1,000건을 3회씩 확인했다.
- `PERF-04A`에서 두 컬럼이 독립적으로 `100 → 200`이 되고, `PERF-04B`에서 진행 중인 2페이지 요청이 취소된 뒤 `김민준` 4건으로 안정되는지 각각 3회씩 확인했다.
- mock API의 임의 `500`은 성능 표본에서 제외했으며 현재 콘솔 오류 2건도 의도된 초기 GET `500`임을 확인했다.
- 재현 가능한 클라이언트 병목이 없어 보드 소스 코드와 의존성을 변경하지 않고 현재 페이지네이션 유지와 가상화 생략을 결정했다.
- `pnpm test:filter`, `pnpm test:pagination`, `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright 세션과 `35555` 서버를 종료하고 이번 검증의 임시 산출물을 휴지통으로 이동했다.

## [performance-requirement] 검색·필터 성능 합격 기준

### 프롬프트

> 각 이슈별 별도의 워크트리에서 작업 진행후 main PR 작업진행한다.
> 모든 이슈 해결까지 진행하고 완료되면 PR 링크만 제공

- 대상: GitHub Issue #17 `검색·필터 성능 요구사항의 정량적 합격 기준 정의`

### AI 출력 요지

- 200건 이상 Must와 1,000건 Should에 공통으로 적용할 프로덕션 Chromium 측정 환경을 명시했다.
- 이름·직무 조건 변경 뒤 요청 시작과 마지막 응답 뒤 화면 반영을 각각 `100ms` 이내로 정하고, 클라이언트 반영 구간 Long Task `0건`을 합격 기준으로 추가했다.
- 1,000건 추가 페이지 로드 중 검색 입력과 다른 컬럼 스크롤이 차단되지 않아야 한다는 Should 기준을 유지했다.
- 기존 성능 시나리오, 화면 완료 기준과 Phase 6 계획의 측정값·판정·완료 조건을 정량 기준에 맞췄다.

### 리뷰 / 검증

- 기존 성공 표본의 요청 시작 지연 최대 `29.8ms`, 클라이언트 반영 최대 `82.2ms`, Long Task `0건`이 새 기준 안에 있는지 대조했다.
- 1,000건 측정이 200건 이상 Must와 1,000건 Should를 함께 검증한다는 범위를 요구사항과 시나리오 양쪽에 명시했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- 공유 `node_modules` 심볼릭 링크로 인한 Turbopack 외부 경로 오류는 워크트리 로컬 의존성 설치 후 재검증해 해소했다.
- `git diff --check`를 통과했다.
- PR 간 병합 감사에서 Issue #20과 `docs/requirements.md`의 인접 Should 항목 충돌을 확인해, 1,000건 정량 기준을 검색·필터 성능 항목 안으로 옮기고 기존 Should 문구를 유지했다.
- 수정 뒤 네 이슈 브랜치의 모든 쌍이 `git merge-tree --write-tree`에서 충돌 없이 병합되는지 확인했다.

## [stage-move-retry-test] PATCH 실패 후 재시도 잠금 해제 검증

### 프롬프트 1

> 1. 7번 이슈 e2e 테스트로 이슈 검증
> 2. 테스트 결과보고

### 프롬프트 2

> 1. 문서 갱신
> 2. 깃 이그노어에 playwright\* 추가
> 3. 이슈 내용 상세히 작성후 클로즈

### AI 출력 요지

- Playwright 실제 브라우저에서 `RACE-03`의 지연 실패, 빠른 연속 확정, 롤백과 성공 재시도를 한 흐름으로 검증했다.
- 추적 소스와 mock 핸들러는 수정하지 않고 브라우저 세션의 `fetch`만 임시 제어해 PATCH 횟수·본문·응답 상태를 고정했다.
- 검증 후 실패 고정, 브라우저 저장 상태와 Playwright 세션을 정리했다.
- 확정된 결과에 맞춰 `DECISIONS.md`와 Phase 6 계획의 경쟁 상태 완료 여부를 갱신하고 `.gitignore`에 `playwright*`를 추가했다.
- GitHub Issue #7 본문을 환경·단계별 결과·정리·회귀 검사·완료 조건으로 상세 갱신하고 `completed`로 종료했다.

### 리뷰 / 검증

- 초기 단계별 인원수 `320 · 120 · 30 · 50 · 480`을 확인하고 `김민준` 지원자의 `서류검토 → 면접` 변경을 사용했다.
- PATCH를 700ms 지연된 `500`으로 고정하고 확인 동작을 동기적으로 두 번 실행했을 때 `{ "stage": "INTERVIEW" }` PATCH가 한 번만 발생했다.
- 저장 중 화면이 `서류검토 319 · 면접 121`, 상세 단계 `면접`, `변경 중…` 버튼 2개로 낙관적 갱신되는지 확인했다.
- 실패 후 `서류검토 320 · 면접 120`, 상세 단계 `서류검토`, 원래 첫 카드 위치로 복구되고 오류 알림이 표시되는지 확인했다.
- 같은 지원자에서 응답을 성공으로 전환해 재시도했을 때 새 PATCH가 정확히 한 번 발생하고 `200`으로 완료됐다. 실패·성공 요청 본문은 모두 `{ "stage": "INTERVIEW" }`였다.
- 성공 후 화면과 `worxphere.applicants.v4` 저장 값이 `INTERVIEW`로 일치하고, 새로고침 뒤에도 `면접` 컬럼과 인원수 `서류검토 319 · 면접 121`이 유지되는지 확인했다.
- 브라우저 저장 키와 임시 `fetch` 제어를 제거하고 초기 인원수 `서류검토 320 · 면접 120`으로 복구했다.
- `pnpm test:stage-move` 4건, `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- `git check-ignore -v`로 `output/playwright/...` 생성물이 `.gitignore`의 `playwright*` 규칙에 의해 제외되는지 확인했다.
- GitHub Issue #7의 상세 본문과 완료 댓글을 다시 조회하고 상태가 `CLOSED`인지 확인했다.

## [phase-6-completion-sync] Phase 6 완료 상태 확정

### 프롬프트 1

> phase 6 체크리스트를 갱신하자 오인 요소를 제거하고 싶다.

### 프롬프트 2

> 모바일은 진행하지 않는다.
>
> 문서 자체를 갱신하는게 목표이다.

### AI 출력 요지

- Phase 6 완료 조건과 전체 회귀 검증에서 모바일·좁은 화면을 필수로 오인할 수 있는 문구를 제거했다.
- 브라우저 회귀 범위를 이미 검증한 데스크톱 `1024×768`, `1440×900`으로 명시했다.
- feature-key별 기록·커밋, 자동 검증, 데스크톱 Must 회귀와 검증 상태 정리를 완료 상태로 갱신했다.
- 애플리케이션 코드는 변경하지 않았다.

### 리뷰 / 검증

- `docs/requirements.md`에서 Phase 6 대상이 선택적인 Should이며 모바일 검증이 요구되지 않는지 확인했다.
- Phase 6 feature-key별 `PROMPTS.md` 기록과 개별 커밋이 존재하는지 대조했다.
- 기존 브라우저 기록의 단계 저장 성공·실패, 검색·필터, 상세 패널, `1024×768`·`1440×900` 데스크톱 검증과 정리 결과를 확인했다.
- 사용자 지시에 따라 시작했던 모바일 검증을 중단하고 브라우저 세션, 서버와 임시 생성물을 정리했다.
- Phase 6 문서에서 모바일·좁은 화면 문구가 명시적인 범위 제외 설명에만 남는지 확인했다.

## [readme-runtime-sync] README 런타임 정보 최신화

### 프롬프트 1

> 10번 이슈 브리핑

### 프롬프트 2

> 10번 바로 문서만 갱신하면 될 것 같은데
> 판단하라

### 프롬프트 3

> 갱신하고 커밋 푸시

### AI 출력 요지

- README의 localStorage 키를 실제 mock 핸들러와 같은 `worxphere.applicants.v4`로 수정했다.
- 개별 테스트 명령 목록에 pre-push가 실행하는 `pnpm test:keyboard`를 추가했다.
- 현재 코드와 일치하는 나머지 mock API 설명과 소스 코드는 변경하지 않았다.

### 리뷰 / 검증

- README의 저장 키를 `src/mocks/handlers.ts`의 `APPLICANTS_STORAGE_KEY`와 대조했다.
- `package.json`의 `test:keyboard` 스크립트와 `test:pre-push` 실행 순서를 README 명령 목록과 대조했다.
- `git diff --check`와 스테이징된 문서 diff 검사를 통과했다.

## [api-response] GET 오류 응답 우선순위 정정

### 프롬프트

> 1. fix/api-response 브랜치 생성
> 2. 작업 후 커밋 푸시
> 3. main <- PR

### AI 출력 요지

- 잘못된 GET 요청이 임의 `500`보다 먼저 `400`으로 처리되도록 요청 검증 뒤로 임의 실패 판정을 이동했다.
- 기존 페이지네이션 검사에 GET 요청 검증과 임의 실패의 실행 순서를 확인하는 회귀 검사를 추가했다.
- 문서에 이미 명시된 API 계약에 구현을 맞추고 새 추상화나 의존성은 추가하지 않았다.

### 리뷰 / 검증

- `rg`로 `shouldFail` 호출 경로를 확인해 PATCH는 이미 요청 검증 뒤에 임의 실패를 적용하고 GET만 순서가 다른지 대조했다.
- 수정 전 `pnpm test:pagination`에서 새 우선순위 검사가 실패하고, 수정 후 같은 검사가 통과하는지 확인했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- `git diff --check`를 통과했다.

## [stage-move-pagination] 단계 이동 후 페이지네이션 정합성 복구

### 프롬프트

> 1. `fix/stage-move-pagination`
> 2. 수정
> 3. 커밋푸시
> 4. maim <- PR

### AI 출력 요지

- 단계 저장 성공 시 오프셋이 당겨진 원본 컬럼의 진행 중 요청을 취소하고, 현재 카드를 보존한 채 1페이지부터 다시 동기화했다.
- 대상 컬럼의 다음 페이지 응답은 이미 표시한 지원자 `id`를 제외하고 합쳐 이동 카드와 서버 페이지가 겹쳐도 중복되지 않게 했다.
- 필터가 변경되는 동안 PATCH가 완료돼도 최신 조회 조건을 사용하도록 현재 페이지 로더를 ref로 유지했다.
- 양쪽 컬럼 전체 초기화와 새 의존성은 추가하지 않았다.

### 리뷰 / 검증

- `moveApplicantToStage`, `restoreApplicantStage`, `saveApplicantStageChange`, `loadStagePage`의 호출·응답 흐름을 대조해 저장 성공 뒤 기존 `nextPage`를 유지한 것이 누락·중복의 원인인지 확인했다.
- 회귀 검사 추가 직후 `pnpm test:stage-move`가 `mergeApplicantPage` 미구현으로 실패하고, 수정 후 기존 4건과 신규 `SM-05`까지 5건이 통과하는지 확인했다.
- Playwright에서 임의 실패를 비활성화한 결정적 조건으로 `김민준`을 `서류검토 → 면접` 이동한 뒤 두 컬럼의 남은 페이지를 모두 불러왔다.
- 서류검토 `total/count/unique = 319/319/319`, 면접 `121/121/121`로 카드 수와 고유 ID 수가 API 합계와 일치하는지 확인했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.

## [card-key] 지원자 카드 복합 key 적용

### 프롬프트

> `${index}-${id}`
>
> 형태로 수정하자
>
> 1. fix/card-key 브랜치 생성, 작업
> 2. 커밋 푸시
> 3. main <- PR
> 4. 이슈 상세히 기재후 클로즈

### 후속 프롬프트

> 머지 안된다 충돌 해결

### AI 출력 요지

- 지원자 카드 목록의 React key를 지원자 `id`에서 배열 위치와 ID를 결합한 `${index}-${applicant.id}`로 변경했다.
- 지원자 선택과 페이지 응답 중복 제거는 안정적인 도메인 식별자인 `applicant.id`를 계속 사용하고, 복합 값은 React 형제 key에만 적용했다.
- 카드가 로컬 상태를 갖지 않아 목록 순서 변경 시 재마운트되는 비용을 허용하고, 중복 ID 입력에서도 key 충돌 경고가 발생하지 않게 했다.
- `origin/main`의 API 단계 전이 검증 변경을 병합하고 양쪽 프롬프트 기록을 모두 보존했다.

### 리뷰 / 검증

- `rg`로 지원자 카드 렌더링 경로가 `applicants.map(renderApplicantCard)` 한 곳인지 확인했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.
- 프로덕션 브라우저에서 mock 저장소 1,000건 중 서류검토 지원자 2명의 ID를 같게 만든 뒤 첫 페이지 카드 100개가 렌더링되는지 확인했다.
- 같은 ID 입력에서 콘솔 오류, 페이지 오류와 React 중복 key 경고가 모두 0건임을 확인하고 테스트 저장소를 제거했다.
- `origin/main` 병합 뒤 공통 `canChangeApplicantStage` 사용과 `${index}-${applicant.id}` key가 함께 유지되고 충돌 표식이 남지 않았는지 확인했다.
- 충돌 해소 상태에서 `pnpm test:pre-push`의 전체 검사와 프로덕션 빌드를 다시 통과했다.

## [validation-api] API 단계 전이 규칙 검증

### 프롬프트

> 이슈 14를 위해 별도의 워크트리에서
> fix/vaildation-api 브랜치 생성 후 작업

### AI 출력 요지

- UI에만 있던 단계 전이 규칙을 공통 도메인 함수로 옮겨 UI와 PATCH mock API가 같은 규칙을 사용하게 했다.
- PATCH가 역행, 단계 건너뛰기, 동일 단계, 최종 상태 변경을 저장 전에 `400`으로 거부하도록 수정했다.
- 허용된 6개 전이와 나머지 19개 거부 전이, API 검증 우선순위를 기존 단계 코드 검사에 추가했다.
- 새 의존성이나 별도 테스트 프레임워크는 추가하지 않았다.

### 리뷰 / 검증

- Issue #14의 허용 전이와 거부 전이를 현재 UI 및 PATCH 핸들러 흐름과 대조했다.
- 회귀 검사 추가 직후 `pnpm test:stage-code`가 공통 전이 함수 미구현으로 실패하고, 구현 후 전체 전이 매트릭스가 통과하는지 확인했다.
- API 전이 검증이 임의 `500` 판정과 `localStorage` 저장보다 먼저 실행되는지 확인했다.
- `3015` 포트의 실제 브라우저에서 `INTERVIEW → DOCUMENT_REVIEW` PATCH가 `400`을 반환하고 메모리 단계와 `localStorage`가 모두 유지되는지 확인했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- 프로덕션 빌드가 Next 생성 타입을 만든 뒤 `pnpm exec tsc --noEmit`와 `git diff --check`를 통과했다.
- Playwright 세션과 `3015` 서버를 종료하고 임시 검증 산출물을 휴지통으로 이동했다.

## [api-failure-requirement] Mock API 오류 우선순위 기준

### 프롬프트

> 각 이슈별 별도의 워크트리에서 작업 진행후 main PR 작업진행한다.
> 모든 이슈 해결까지 진행하고 완료되면 PR 링크만 제공

- 대상: GitHub Issue #19 `Mock API 임의 실패 적용 범위와 오류 우선순위 명확화`

### AI 출력 요지

- 약 15% 임의 `500`은 계약과 도메인 규칙을 통과한 유효한 요청에만 적용하도록 기준을 명확히 했다.
- 목록 조회와 단계 저장의 검증·임의 실패 판정 순서를 현재 mock API 흐름대로 문서화했다.
- 요구사항, 화면 명세, README와 설계 결정의 표현을 같은 규칙으로 맞췄다.
- `400/404`가 임의 `500`보다 먼저 반환되는지 완료 확인 기준에 추가했다.

### 리뷰 / 검증

- `src/mocks/handlers.ts`에서 목록 쿼리 검증과 단계 저장의 지원자 존재·본문·전이 검증이 `shouldFail()`보다 먼저 실행되는지 대조했다.
- `pnpm test:pre-push`의 lint, Node 테스트 5종과 프로덕션 빌드가 모두 통과했다.
- 공유 `node_modules` 심볼릭 링크로 인한 Turbopack 외부 경로 오류는 워크트리 로컬 의존성 설치 후 재검증해 해소했다.
- `git diff --check`를 통과했다.

## [stage-undo-api] 실행 취소 API 계약 복구

### 프롬프트

> Audit GitHub Issue #20 and merged PR #21 end-to-end in the existing dedicated worktree `/Users/smlee/worxphere-test-issue-20`.
> If you find a real gap, switch this worktree to a new branch `codex/issue-20-followup` from latest `origin/main`, implement only the fix, test, commit, push, and create a ready PR to main.

### 후속 프롬프트

> Keep ordinary backward transitions rejected; use the minimum explicit undo-only API contract, allow only the immediate reverse of a non-terminal transition, and add a runnable regression check proving normal backward PATCH=400 while explicit undo succeeds and terminal undo stays rejected.

### AI 출력 요지

- 최신 `main`의 UI가 실행 취소 PATCH를 보냈지만 API의 공통 단계 전이 검증이 모든 이전 단계 요청을 `400`으로 거부하는 회귀를 확인했다.
- 일반 단계 변경 계약은 유지하고 `undo: true`를 명시한 요청만 바로 이전 비종료 단계로 복구하도록 공통 계약, mock API와 UI 호출을 맞췄다.
- 최종합격·불합격 실행 취소와 명시되지 않은 이전 단계 변경은 계속 `400`으로 거부한다.
- 다른 지원자의 실행 취소 기록은 유지하되 최종합격·불합격 지원자 상세를 연 동안에는 실행 취소 상태 카드를 숨겼다.

### 리뷰 / 검증

- 수정 전 port `3004` 브라우저에서 전진 PATCH `200` 뒤 실행 취소 PATCH가 세 번 연속 `400`으로 거부되는 것을 재현했다.
- `pnpm test:stage-code`에 일반 이전 단계 거부, 두 비종료 단계의 명시적 실행 취소 허용과 종료 단계 실행 취소 거부 회귀 검사를 추가했다.
- `pnpm test:pre-push`, port `3004` 브라우저의 실행 취소 성공·실패 롤백·새로고침 유지와 최종합격·불합격 상세의 실행 취소 숨김, `git diff --check`를 검증했다.

## [stage-move-consistency] 단계 저장 중 화면 일관성 복구

### 프롬프트

> P1 수정해

### AI 출력 요지

- 단계 PATCH가 진행되는 동안 이름 검색과 직무 필터를 비활성화하고 이벤트 핸들러도 변경을 거부해, 저장 전 목록 응답이 낙관적 이동 결과를 덮어쓰지 못하게 했다.
- 낙관적으로 최종합격 또는 불합격 단계가 표시돼도 상세 패널의 두 단계 액션을 유지하고, 두 버튼을 `변경 중…` 상태로 비활성화했다.
- 새 상태·컴포넌트·의존성 없이 기존 지원자별 저장 상태와 shadcn/ui `Button`을 재사용했다.
- 저장 중 필터 잠금과 액션 유지 조건을 기존 단계 이동 Node 검사에 추가했다.

### 리뷰 / 검증

- `pnpm test:stage-move`에서 신규 `SM-06`을 포함한 6개 검사가 통과했다.
- `pnpm test:pre-push`, `pnpm test:hooks`, `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.
- 프로덕션 브라우저에서 `처우협의 → 최종합격` PATCH를 지연하고, 저장 중 `변경 중…` 버튼 2개가 모두 비활성화되는지 확인했다.
- 같은 저장 구간에서 이름 검색과 직무 체크박스 4개가 비활성화되고 `단계 변경을 저장하고 있습니다.` 상태 문구가 표시되는지 확인했다.
- PATCH 성공 후 이동 지원자가 최종합격 컬럼에 한 번만 남고 필터가 다시 활성화되며 저장 중 버튼과 오류 알림이 사라지는지 확인했다.
