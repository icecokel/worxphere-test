# AI 협업 기록

## [project-setup] 프로젝트 기반 설정

### 프롬프트 1

> 프로젝트 세팅한다.
>
> 1. nextjs, ts, tailwind
> 2. shadcn
> 3. msw

### AI 출력 요지

- Next.js App Router와 TypeScript, Tailwind CSS 프로젝트를 생성했다.
- shadcn/ui를 초기화하고 기본 Button 컴포넌트를 추가했다.
- MSW worker와 브라우저 초기화 파일을 구성했다.

### 리뷰 / 검증

- Next.js 기본 예제 화면과 프로젝트에 불필요한 안내 링크를 제거했다.
- 실제 백엔드가 없는 과제이므로 MSW 시작이 끝난 뒤 애플리케이션을 표시하도록 구성했다.
- MSW 자동 생성 worker는 ESLint 검사에서 제외하고 애플리케이션 코드는 `pnpm lint`로 검증했다.
- `pnpm build`로 타입 검사와 프로덕션 빌드가 통과하는지 확인했다.

## [workflow-guardrails] 기능 단위 커밋 자동화

### 프롬프트 1

> - 하나의 기능을 하나의 커밋으로 기록한다.
> - 커밋을 squash하거나 force-push로 정리하지 않는다.
> - `PROMPTS.md`에 기능별 프롬프트와 AI 출력에 대한 실제 리뷰·검증 내용을 남긴다.
>
> 이 부분을 스킬과 훅으로 만들어 자동으로 세팅되도록하자.

### AI 출력 요지

- 기능 키를 커밋과 `PROMPTS.md`에서 일치시키는 프로젝트 스킬을 추가했다.
- 기능 커밋의 메시지와 프롬프트 로그 구조를 검사하는 `commit-msg` 훅을 추가했다.
- 비 fast-forward push를 막는 `pre-push` 훅과 자동 설치 스크립트를 추가했다.

### 리뷰 / 검증

- 하나의 커밋이 의미적으로 하나의 기능인지는 훅으로 정확히 판별할 수 없어 스킬의 판단 규칙으로 남겼다.
- squash만 선별해 차단하는 것은 신뢰할 수 없어 전체 rebase를 막지 않고 스킬 규칙으로 유지했다.
- force-push 결과인 비 fast-forward 갱신은 `pre-push`에서 차단하도록 했다.
- `pnpm test:hooks`로 올바른 로그가 있는 커밋과 fast-forward push는 통과하고, 범위·로그가 없는 기능 커밋과 비 fast-forward push는 실패하는지 검증했다.
- 스킬 validator와 `pnpm lint`를 통과했으며, 설치된 훅 파일의 실행 권한과 `core.hooksPath` 설정을 확인했다.

### 프롬프트 2

> agents.md에 추가해서 일관되도록 하고

### AI 출력 요지

- 기능 단위 커밋, `feature-key`, `PROMPTS.md`, squash·force-push 규칙을 `AGENTS.md`에 추가했다.
- 스킬과 훅이 담당하는 범위를 `AGENTS.md`에서도 동일하게 명시했다.

### 리뷰 / 검증

- Next.js가 관리하는 자동 생성 블록은 수정하지 않고 프로젝트 규칙을 블록 밖에 추가했다.
- `atomic-feature-commits` 스킬, Git 훅, `AGENTS.md`의 규칙이 서로 모순되지 않는지 대조했다.

### 프롬프트 3

> 커밋

### AI 출력 요지

- 현재 변경을 논리 단위별 커밋으로 분리했다.
- 커밋 후 훅 테스트를 다시 실행해 추적된 `PROMPTS.md`가 테스트 인덱스에서 변경으로 잡히지 않는 문제를 발견했다.

### 리뷰 / 검증

- 실제 작업 트리를 수정하지 않고 대체 인덱스에 테스트용 `PROMPTS.md` blob을 등록하도록 수정했다.
- 기존 커밋을 squash하거나 수정하지 않고 별도의 수정 커밋으로 검증 과정을 보존했다.

## [screen-spec] 파이프라인 화면 설계

### 프롬프트

> 요구사항 문서를 보고 작업 전 와이어프레임을 Mermaid로 작성한다.
>
> 나인하이어 ATS를 레퍼런스로 사용하고, 작업자가 바로 이해할 수 있도록 화면 명세를 자세히 작성한다.

### AI 출력 요지

- 단계별 컬럼, 지원자 카드, 검색·필터, 상세 패널과 화면 상태를 Mermaid 와이어프레임으로 정리했다.
- 레이아웃, 표시 정보, 상호작용, 낙관적 업데이트, mock API 계약과 완료 확인 기준을 문서화했다.

### 리뷰 / 검증

- `docs/requirements.md`의 Must와 화면 명세를 대조했다.
- 나인하이어 화면의 카드형 정보 구조만 채택하고 범위 밖인 공고 관리, 평가, 메시지, 일정 기능은 제외했다.
- 요구사항에 없는 뒤로 가기, 공고 상태, 상대 시간 정보를 와이어프레임에서 제거했다.
- 저장소 검색 결과 기존 공용 UI는 `Button`만 확인되어, 나머지는 필요한 shadcn/ui 구성요소만 추가하도록 명시했다.

## [implementation-plan] Phase별 구현 계획

### 프롬프트

> 와이어프레임 기준 작업 계획서를 `docs/plan`에 작성한다.
>
> Phase와 Task로 나누고 범위가 크면 Phase별 파일로 분리한다.

### AI 출력 요지

- 전체 인덱스와 5개 Phase 실행 문서로 계획을 분리했다.
- 각 Task에 feature-key, 예상 파일, 작업 체크리스트, 검증, 커밋 경계를 명시했다.

### 리뷰 / 검증

- 요구사항, 와이어프레임, 설계 결정을 기준 문서로 고정했다.
- 모든 Must 요구사항을 담당 Task에 연결하고 Should는 Phase 5 이후로 분리했다.
- 기존 `Button` 우선 재사용, 필요한 shadcn/ui만 추가, 별도 DnD 라이브러리 미사용 원칙을 반영했다.
- Phase 간 선행조건과 기능 단위 커밋 순서가 일치하는지 대조했다.

## [coding-conventions] 코딩 컨벤션 정의

### 프롬프트

> 코딩 컨벤션 작성
>
> 1. 파일명을 케밥을 유지한다.
> 2. 배럴구조를 사용하지 않는다.
> 3. 익명 함수 사용하지 않는다.

### AI 출력 요지

- 파일명, 모듈 내보내기, 함수 작성 규칙을 `docs/coding-conventions.md`에 정의했다.
- 코딩 작업 시 항상 적용되도록 `AGENTS.md`에 동일한 핵심 규칙을 추가했다.

### 리뷰 / 검증

- 사용자가 제시한 세 가지 규칙만 문서화하고 추가 스타일 규칙은 만들지 않았다.
- 프레임워크가 요구하는 고정 파일명만 케밥 케이스 규칙의 예외로 명시했다.
- 배럴 구조와 익명 함수의 금지 범위를 직접 import와 이름 있는 콜백까지 구체화했다.

## [shadcn-default-design] shadcn 기본 디자인 규칙

### 프롬프트

> 디자인은 주요 요건이 아니므로 shadcn의 기본 디자인을 적극적으로 활용하고 스타일이 유지되도록 디자인 스킬을 생성한다.

### AI 출력 요지

- UI 구현·리뷰에 자동 적용되는 `shadcn-default-design` 프로젝트 스킬을 추가했다.
- `base-nova`, `neutral`, semantic token과 shadcn/ui 기본 variant를 우선하는 규칙을 정의했다.
- 화면 명세와 구현 계획의 시각 기준을 같은 원칙으로 정리했다.

### 리뷰 / 검증

- 기존 `shared-ui-first`는 컴포넌트 선택, 새 스킬은 시각적 일관성을 담당하도록 역할을 분리했다.
- 나인하이어 레퍼런스는 정보 구조에만 적용하고 브랜드 시각 복제는 제외했다.
- 요구사항이 없는 커스텀 팔레트, 폰트, 그림자, radius, animation을 만들지 않도록 제한했다.
- 단일 `SKILL.md`로 충분해 스크립트, 레퍼런스, UI 메타데이터는 추가하지 않았다.

## [mock-api] 지원자 조회와 단계 저장 API

### 프롬프트

> 1. feature/msw/mock-api
> 2. 작업 시작한다
> 3. 타입 스트립트로 스키마 명시한다.
> 4. 공통 스키마, 요청, 응답 스키마 명시 후 상속 받는구조

### 프롬프트 2

> 지원자 1000건 이상으로 만들어야 으로
> 몇가지 규칙으로 생성되도록 하는게 좋다.
>
> 뮤테이션이 일어날 예정이기때문에, 상태변환
> 고려해서 작업을 진행해야 한다.

### 프롬프트 3

> 일반적으로 1년이치를 잘 안본다고 판단된다.
> 기간은 30일로 고정한다.
>
> 이름이 반복해서 나올 수 있음으로,
> 이름 생성 또한 조합으로 진행한다.
> 15개의 성씨 와 15개 이상의 이름으로 조합을한다.

### AI 출력 요지

- 공통 지원자 스키마와 단계 변경 요청·응답, 목록·오류 응답 스키마를 TypeScript로 정의했다.
- 순번 ID, 15개 성씨와 20개 이름 조합, 직무·단계 순환, 최근 30일 분산 규칙으로 1,000명의 초기 지원자를 생성했다.
- GET·PATCH MSW 핸들러에 지연, 임의 실패, 요청 검증과 성공 상태 저장을 구현했다.
- 단계 변경을 기존 배열을 수정하지 않는 상태 변환 함수로 분리했다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`를 통과했다.
- Playwright 브라우저에서 GET 성공 시 1,000건과 GET 실패 시 `500` 응답을 확인했다.
- 생성된 1,000건의 ID가 모두 고유하고 순번·직무·단계·지원일 분산 규칙을 따르는지 확인했다.
- 15개 성씨와 20개 이름이 300개 조합을 만들고 지원일이 30개 날짜에 분산되는지 확인했다.
- PATCH 성공 응답과 저장 단계가 일치하고 새로고침 후에도 유지되는지 확인했다.
- PATCH 임의 실패 시 기존 저장 단계가 유지되는지 확인했다.
- 존재하지 않는 지원자의 `404`, 허용되지 않은 단계의 `400`, 손상되거나 1,000건 미만인 저장 데이터의 초기값 복구를 확인했다.

## [phase-completion-criteria] Phase 완료 기준 보강

### 프롬프트

> 모든 Phase 문서에는 목표와 완료 기준이 있어야 한다. 부족한 부분을 개선해 문서를 갱신한다.

### AI 출력 요지

- 5개 Phase 문서의 목표 아래에 결과 중심의 완료 기준 체크리스트를 추가했다.
- 완료 기준은 완료 여부 판단, 기존 Phase 검증은 실행 절차 확인으로 역할을 구분했다.

### 리뷰 / 검증

- 모든 `phase-*.md`에 `## 목표`와 `## 완료 기준`이 각각 하나씩 있는지 확인했다.
- 기존 Task와 검증 절차, Must·Should 범위, mock API 진행 상태를 변경하지 않았다.

## [pipeline-board] 읽기 전용 파이프라인 보드

### 프롬프트

> 1. feature/pipeline-board 브랜치 생성하고
> 2. 작업 시작
> 3. 목표에 완수했는지 코드레벨에서 확인
>
> 100개씩 페이지네이션 하고,
> 섹션 별로 스크롤링 만들고
> 인피니티 스크롤로 진행한다.

### AI 출력 요지

- `feature/pipeline-board` 브랜치를 만들고 `/`에 읽기 전용 파이프라인 보드를 연결했다.
- 공용 `STAGES` 순서로 5개 컬럼과 지원자 카드를 표시하고, 지원일 형식·컬럼 인원수·부분 및 전체 빈 상태를 구현했다.
- `loading`, `success`, `error` 상태와 요청 취소, 실패 후 같은 GET 요청을 실행하는 재시도를 구성했다.
- 단계별 지원자를 100개 단위로 조회하고 각 컬럼 하단에서 다음 페이지를 불러오는 독립 스크롤을 구성했다.
- 기존 `Button`을 재사용하고 shadcn/ui Card와 Skeleton만 추가했다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright에서 초기 로딩 중 5개 컬럼 스켈레톤이 표시되는지 확인했다.
- 고정 응답으로 컬럼 순서, 카드 위치와 필수 정보, `288px` 컬럼 너비, `12px` 간격, 컬럼별 인원수를 확인했다.
- GET 실패 문구와 기존 `Button` 기반 재시도 후 성공 전환을 확인했다.
- 전체 빈 상태의 모든 인원수 `0`과 `등록된 지원자가 없습니다.`, 일부 빈 컬럼의 `지원자 없음`을 구분해 확인했다.
- `375px` 화면에서 페이지 자체는 넘치지 않고 보드 영역만 가로 스크롤되는지 확인했다.
- 초기 구현에서 실제 MSW 응답의 1,000개 카드가 단계별 200개씩 표시되는지 확인했다.
- `pnpm test:pagination`으로 100개 단위 분할, 전체 인원수, 마지막 페이지 종료 조건을 확인했다.
- 추가 요구사항 적용 후 실제 MSW 응답에서 단계별 최초 100개만 표시되고 한 컬럼 하단 스크롤 시 해당 컬럼만 200개로 증가하는지 확인했다.
- 브라우저 콘솔 오류·경고가 없는지 확인했다.
- 검색·직무 필터, 상세 패널, 카드 선택·드래그·저장은 Phase 2 범위 밖이므로 포함하지 않았다.

## [applicant-stage-distribution] 초기 지원자 단계 분포 조정

### 프롬프트

> 데이터가 너무 정직하게 200개씩 끊어져 있는데 왜그럴까?
>
> 현실적인 비율 상정해봐,.
>
> api 갱신

### AI 출력 요지

- 5개 단계를 20%씩 순환 배치하던 초기 데이터 생성 규칙을 채용 퍼널 형태의 비율 배치로 변경했다.
- 1,000명을 `서류검토 320명 · 면접 120명 · 처우협의 30명 · 최종합격 50명 · 불합격 480명`으로 생성한다.
- 기존 브라우저 저장 데이터가 균등 분포를 계속 복원하지 않도록 저장 키를 버전업했다.

### 리뷰 / 검증

- `pnpm lint`, `pnpm build`, `pnpm test:pagination`, `git diff --check`를 통과했다.
- 실제 MSW에서 단계별 `total`이 `320 · 120 · 30 · 50 · 480`이고 합계가 1,000명인지 확인했다.
- 기존 페이지네이션 관련 미커밋 변경은 수정하거나 스테이징하지 않았다.

## [candidate-filter] 지원자 이름 검색과 직무 필터

### 프롬프트

> 페이즈 3 문서 읽는다.
>
> 1. feature/search-detail 브랜치 생성
> 2. 작업 시작

### 프롬프트 2

> 목적 기준에 완수 했는가
>
> 보완 시작

### 프롬프트 3

> 검색운 페이지 네이션이 있음으로 api로 진행해야한다.
>
> 직무 UI를 체크 박스로 진행한다.

### 프롬프트 4

> 체크박스를 클릭 할 때 마다 api 호출해서 신선한 데이터 상태를 유지 해야 한다.

### 프롬프트 5

> 체크박스 기본값은 전체 체크드이다.

### AI 출력 요지

- `feature/search-detail` 브랜치를 만들고 이름 검색 입력과 로드된 데이터 기반 직무 선택을 추가했다.
- 이름 부분 검색과 직무 정확 일치 조건을 AND로 적용하고, 표시 카드와 컬럼 인원수를 같은 필터 결과에서 계산했다.
- 별도 검색 라이브러리나 디바운스 없이 `useMemo`, `Set`, 표준 배열 메서드를 사용했다.
- 기존 shadcn/ui 스타일을 유지하는 `Input`, `Select`만 추가하고 필터의 빈 결과 상태를 구현했다.
- 무쿼리 `GET /api/applicants`로 전체 지원자 검색 인덱스를 한 번 불러와 컬럼 페이지네이션에 아직 포함되지 않은 지원자도 검색되게 보완했다.
- 검색 인덱스 조회 실패 시 필터를 비활성화하고 기존 `Button`으로 재시도할 수 있게 했다.
- 전체 검색 인덱스 선로딩을 제거하고 이름·직무 조건을 단계별 GET API 쿼리로 전달하도록 보정했다.
- mock API가 전체 지원자에서 이름·직무·단계를 먼저 필터한 뒤 100건씩 페이지네이션하도록 변경했다.
- 필터 변경 시 이전 요청을 취소하고 각 컬럼을 1페이지부터 다시 조회하며, 직무 선택지는 최초 API 응답에서 누적해 유지했다.
- 단일 직무 `Select`를 shadcn/ui 체크박스로 바꾸고 선택 없음은 전체, 여러 직무 선택은 OR 조건으로 처리했다.
- 선택한 직무는 반복 `role` 쿼리 파라미터로 전달하고 mock API에서 모두 읽도록 변경했다.
- 체크 상태가 바뀔 때마다 이전 요청을 취소하고 `cache: "no-store"`로 각 컬럼의 1페이지를 다시 조회하도록 명시했다.
- 모든 직무 체크박스를 기본 선택 상태로 표시하고, 일부 해제 시 남은 직무만 API 조건으로 전달하도록 변경했다.

### 리뷰 / 검증

- `pnpm test:filter`로 한글 부분 검색, 앞뒤 공백 제거, 영문 대소문자 무시, 다중 직무 OR, 이름·직무 AND 조건과 초기화를 확인했다.
- 체크박스 전환 후 `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `git diff --check`를 통과했다.
- 체크박스 재조회에 `cache: "no-store"`를 적용한 뒤 같은 검증을 다시 통과했다.
- Playwright에서 이름 `민준` 검색과 `백엔드 개발자` 필터를 함께 적용했을 때 카드와 컬럼 인원수가 함께 줄어드는지 확인했다.
- 실제 MSW 응답에서 380건이 로드된 상태로 필터가 즉시 반영되고, 결과가 없을 때 5개 컬럼과 `조건에 맞는 지원자가 없습니다.`가 유지되는지 확인했다.
- 검색과 직무를 초기화하면 단계별 전체 인원수 `320 · 120 · 30 · 50 · 480`이 복원되는지 확인했다.
- `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- 검색 인덱스 최초 조회가 임의 실패했을 때 오류 문구와 `다시 시도`가 표시되고 재시도 후 필터가 활성화되는지 확인했다.
- 최초 컬럼 페이지에 없는 `applicant-0121`, `applicant-0421`, `applicant-0721`이 이름 `김서준` 검색 결과로 각각 표시되는지 확인했다.
- 전체 인덱스 결과 카드에서도 지원자 상세 패널이 열리는지 확인했다.
- 보완 후 `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright에서 이름 `김서준` 검색이 최초 컬럼 페이지 밖의 `applicant-0121`, `applicant-0421`, `applicant-0721`을 API 응답으로 표시하는지 확인했다.
- `프론트엔드 개발자` 필터에서 불합격 컬럼의 API 결과가 총 120건이고, 최초 100건 뒤 다음 페이지 20건을 이어서 표시하는지 확인했다.
- 전체 인덱스 선로딩 요청과 클라이언트 필터 계산이 제거됐는지 코드 수준에서 확인했다.
- 통합 후 `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.
- Playwright에서 프론트엔드·백엔드 동시 선택 시 불합격 합계가 240건으로 OR 적용되고, 스크롤 후 2페이지까지 200건이 이어 붙는지 확인했다.
- 같은 다중 직무 조건에서 이름 `김서준`을 함께 적용해 최초 페이지 밖의 지원자가 이름·직무 AND 조건으로 표시되는지 확인했다.
- 새로고침 직후 네 직무 체크박스가 모두 선택되고, 프로덕트 매니저 해제 시 나머지 세 직무 기준으로 API 합계가 갱신되는지 확인했다.

## [candidate-detail] 지원자 상세 패널

### 프롬프트

> 페이즈 3 문서 읽는다.
>
> 1. feature/search-detail 브랜치 생성
> 2. 작업 시작

### AI 출력 요지

- 지원자 카드 선택 상태를 객체 복사본이 아닌 `id`로 보관하고, 선택한 지원자의 필수 정보를 우측 Sheet에 표시했다.
- 카드에 지원자 이름을 포함한 접근 가능한 레이블과 포커스 링을 추가했다.
- 닫기 버튼, `Escape`, 바깥 영역 선택으로 패널을 닫고 검색·필터 결과에서 선택한 지원자가 제외돼도 닫히도록 했다.
- shadcn/ui Sheet 기본 동작과 스타일을 유지하면서 데스크톱 `420px`, `640px` 미만 전체 너비만 적용했다.

### 리뷰 / 검증

- Playwright에서 `김민준`, `이민준` 카드를 각각 선택해 이름·직무·지원일·현재 단계가 카드와 일치하는지 확인했다.
- 닫기 버튼, `Escape`, 패널 바깥 영역 선택이 각각 패널을 닫는지 확인했다.
- 선택한 `김민준`이 제외되도록 이름 필터 상태를 변경했을 때 패널이 닫히는지 확인했다.
- 계산된 패널 너비가 1280px 화면에서 `420px`, 375px 화면에서 `375px`인지 확인했다.
- 패널 닫기는 선택 `id`만 초기화하고 보드 데이터와 스크롤 DOM은 유지하는지 코드 수준에서 확인했다.
- `pnpm test:filter`, `pnpm lint`, `pnpm build`, `git diff --check`를 통과했다.

## [stage-move] 카드 단계 이동과 실패 롤백

### 프롬프트

> 문서 기준 작업 시작

### AI 출력 요지

- HTML Drag and Drop API로 지원자 ID를 전달하고 다른 단계 컬럼만 유효한 drop target으로 처리했다.
- 드롭 즉시 같은 보드 상태에서 카드, 컬럼 인원수, 상세 패널 단계를 갱신하고 `PATCH /api/applicants/:id/stage`를 한 번 호출했다.
- 저장 중인 지원자 ID만 `Set`으로 관리해 카드에 투명도와 `저장 중…`을 표시하고 추가 드래그를 막았다.
- PATCH 실패 시 이동 전 단계와 배열 위치를 복원하고 기존 `Card`, `Button`으로 닫기와 자동 소멸이 가능한 `role="alert"` 알림을 표시했다.
- 드래그와 짧은 선택을 구분하고 드래그 중 ID는 동기 ref에 보관해 빠른 `dragover`에서도 drop 허용이 누락되지 않게 했다.
- 별도 DnD 라이브러리, 새 의존성, 범용 상태 추상화는 추가하지 않았다.

### 리뷰 / 검증

- Playwright에서 5개 단계의 서로 다른 방향 20개 조합을 이동하고 각 PATCH가 `200`으로 완료되는지 확인했다. 1280px에서 화면 밖인 양 끝 컬럼 조합은 1920px에서 확인했다.
- 같은 컬럼 drop 전후 네트워크 기록에 PATCH가 추가되지 않고 카드 선택 패널도 열리지 않는지 확인했다.
- 저장 중 카드가 `aria-busy="true"`, `draggable=false`, `저장 중…` 상태이며 두 번째 대상 컬럼으로 이동되지 않고, 완료 후 다시 드래그 가능해지는지 확인했다.
- 성공한 PATCH 후 새로고침해 원본 컬럼 `320 → 319`, 대상 컬럼 `120 → 121`과 이동한 카드 단계가 유지되는지 확인했다.
- 이름 `김민준` 검색 중 이동해 카드와 두 컬럼 인원수가 함께 갱신되는지 확인했다.
- 런타임에서 임의 실패를 강제로 발생시켜 카드와 인원수의 원래 위치 복원, 실패 알림 표시, 재이동 가능 상태를 확인하고 강제 설정을 복원했다.
- 저장 중 이동 카드의 상세 패널이 대상 단계 `면접`을 즉시 표시하고, 강제 실패 후 원래 단계 `서류검토`로 복구되는지 확인했다.
- 실패 알림의 직접 닫기 전후 개수가 `1 → 0`이 되고, 직접 닫지 않은 알림은 일정 시간 뒤 사라지는지 확인했다.
- 드래그 중 대상 컬럼에 `border-primary`, `bg-secondary`가 적용되는지 확인했다.
- `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.

### 프롬프트 2

> 지원자 이동을 DND 가 아닌 상세해서 상태 변경으로 바꾸는 흐름으로 바꾼다.
>
> - 지원자에게는 중요한 결정이고, 결정에 따라 지원자에게 알림이 가는 복잡성을 고려해야한다.
>
> 단계별로 하나씩 넘어가야 한다.
>
> 불합격은 언제나 할 수 있다.
>
> 서류검토 -> 면접 -> 처우협의 -> 합격
>
> ---
>
> 불합격
>
> 1. 섹션을 나눠서 상태를 변경 할 수 있게한다.
> 2. 단계를 선택하는 것이 아닌 현재 상태에 맞춰 세팅되도록

### AI 출력 요지

- 카드 드래그앤드롭과 단계 선택 UI를 제거하고, 상세 패널에서만 상태를 변경하도록 전환했다.
- 상세 패널을 `다음 채용 단계`와 `불합격 처리` 섹션으로 나눴다.
- 정규 전이는 `서류검토 → 면접 → 처우협의 → 최종합격`의 바로 다음 단계만 제공하고, 불합격은 이미 불합격이 아닌 모든 상태에서 별도 처리할 수 있게 했다.
- 변경 전 지원자 이름, 이전·대상 단계, 알림 발송 가능성을 확인받고, 기존 낙관적 저장·실패 롤백·오류 알림을 유지했다.
- 실제 알림 발송은 범위 밖으로 두고, 변경 영향 가능성만 고지했다.

### 리뷰 / 검증

- Playwright에서 한 지원자를 `서류검토 → 면접 → 처우협의 → 최종합격` 순서로 변경하고 각 PATCH 성공을 확인했다.
- 최종합격 상태에서도 별도 `불합격 처리` 액션이 제공되는지 확인했고, PATCH 실패 시 최종합격으로 롤백된 뒤 재시도로 불합격 저장과 새로고침 후 유지가 되는지 확인했다.
- 확인창을 취소했을 때 화면 변경과 PATCH 요청이 생기지 않는지, 이미 불합격인 지원자에게 정규 진행 단계가 제공되지 않는지 확인했다.
- `pnpm test:filter`, `pnpm test:pagination`, `pnpm test:hooks`, `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm build`, `git diff --check`를 통과했다.
